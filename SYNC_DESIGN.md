# AA4C 同步与信任分级设计（V0.2）

> 状态：**设计草案**，对应 [ROADMAP.md](ROADMAP.md) V0.2。本文档是落地前的设计依据，不含实现代码。
> 关联：信任分级权威定义见 [PROJECT_VISION.md](PROJECT_VISION.md) §十；表结构见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)；线路协议见 [PROTOCOL.md](PROTOCOL.md)；界面见 [UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md)。

## 1. 背景与目标

V0.1 的设备信任是二元的（配对 / 未配对），文件传输是一次性的（发完即止、互不关联）。V0.2 要把"自己的多台设备"变成**一个统一的文件空间**：

1. **信任分级**：区分自己的设备、朋友、临时、陌生，权限不同。
2. **跨设备文件索引**：把完全信任设备上的文件名/目录汇总成一个视图，按"能不能拿到"用颜色标识。
3. **按需获取**：默认只同步元数据（轻），文件内容点了才拉，省空间省流量。
4. **Inbox（收到的）**：AA 收到的文件统一进收件箱，并纳入索引——在 A 收到，在 B 也看得到、能拉。

设计原则延续 [AGENTS.md](AGENTS.md)：稳定 > 功能，简单 > 复杂，默认安全。

## 2. 信任分级（4 级）

权威定义见 [PROJECT_VISION.md](PROJECT_VISION.md) §十；此处给同步视角的摘要：

| 层级 | 含义 | 入库 | 参与跨设备索引 | 收文件 |
|------|------|------|----------------|--------|
| 完全信任 | 自己的多台设备 | ✅ | ✅ 双向 | 可设为免确认 |
| 朋友 | 朋友 / 家庭 / 团队 | ✅ | ❌（仅手动分享，归 V0.3 Share） | 需确认 |
| 临时 | 单次收发对象 | ❌ | ❌ | 当次需确认 |
| 陌生 | 已发现未配对 | ❌（仅内存） | ❌ | 拒收 |

- 配对成功默认 **朋友**；用户在设置里把"自己的设备"**升级为完全信任**才进入索引/同步。
- 只有 **完全信任** 设备之间才交换文件索引、才允许按需拉取。这是隐私的关键边界。
- **降级即清理**：把某设备从完全信任降为朋友（或解除配对）时，**立即清空该设备在本机缓存的远端索引**（`remote_index` 中 `device_id` 的全部行），其条目从统一视图消失；本机已落地的文件不动。

## 3. 跨设备文件索引

### 3.1 共享范围（sync scope）

完全信任设备各自声明若干"共享范围"：

- **同步目录**：用户选定纳入同步的文件夹（照片库、知识库、模型库、项目目录等）。
- **Inbox（收到的）**：AA 收到的文件默认落点，自动作为一个共享范围（见 §5）。

### 3.2 本地索引

每台设备扫描自己共享范围内的文件，维护本地索引：`rel_path / size / mtime / blake3 / present_local`。`present_local=1` 表示内容确实在本机磁盘上（区别于"只是索引里有"）。

**维护策略 = 文件监听 + 定时扫描兜底**：

- 实时用文件系统监听（`notify` crate）捕捉增删改，秒级更新索引并广播变更。
- 监听有盲区（漏事件、外部挂载、批量操作、移动端后台受限），故另有**定时全量/增量扫描**（默认间隔可配，如启动时 + 每若干小时）按 `mtime`/`size` 比对补漏。
- 哈希惰性计算：扫描先比 `mtime`+`size`，疑似变化才重算 BLAKE3，避免大目录每次全量哈希。

### 3.3 上线交换

设备上线后，与**当前在线**的完全信任设备交换**索引摘要**（文件名、目录、大小、哈希），**不传内容**。摘要量小，可频繁同步。

### 3.4 统一视图

界面展示的是"本人所有完全信任设备索引的**并集**"：同一文件（按 `rel_path`+`hash` 归并）可能同时存在于多台设备。每个条目按可获取性着色（§4）。

**同名不同内容（多版本）= 加序号区分，绝不自动覆盖**：同一 `rel_path` 出现多个不同 `hash` 时，并列展示并以序号区分（如 `报告.pdf`、`报告 (2).pdf`），各自独立着色；同时记入 `sync_conflicts`（§8）。拉取/打开针对具体某一版本，用户决定保留哪份。

## 4. 文件状态模型（绿 / 黄 / 红）

这是本设计的核心可视化。状态对**当前这台设备**而言，按"本地是否有 + 远端在场情况"判定：

| 颜色 | 含义 | 判定 |
|------|------|------|
| 🟢 绿 | 本地已有 | `present_local = 1`（已下载，或本地新增） |
| 🟡 黄 | 可下载 | 本地没有，但**至少一台在线**完全信任设备有 |
| 🔴 红 | 暂不可取 | 本地没有，且**仅离线**完全信任设备有 |

- "在线/离线"由设备发现（mDNS `last_seen_at`，30s 判定，沿用 V0.1）驱动；设备一上线，红 → 黄。
- **按需拉取**：点黄色条目 → 选一台在线持有设备 → 复用 ATP（[PROTOCOL.md](PROTOCOL.md) v1 的 `Offer`/分块/BLAKE3 校验）拉取 → 落地后 `present_local=1` → 转绿。
- 反向："本地新增"文件即刻是绿；它的索引摘要广播出去后，在别的设备上呈现为黄。

## 5. Inbox（收到的）

- AA 收到的文件默认落到 **Inbox 目录**（设置可改），不再散落到任意接收目录。
- Inbox 是一个共享范围 → 纳入索引 → 在你其他完全信任设备上呈现为黄、可一键拉取。
- 效果：用手机收了朋友发来的文件，回到电脑前它已经在"收到的"里显示为黄，点一下就拉到电脑。
- **分组展示**：Inbox 内按"来源设备 + 时间"分组（如「来自 客厅电脑 · 今天」），便于回看是谁、什么时候发来的；底层仍是同一索引，分组只是视图。

## 6. 数据模型

落地表结构见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md) §4（V0.2）。要点：

- `devices.trust_level`：`'full' | 'friend'`（临时/陌生不入库）。
- `sync_scopes`：共享范围（kind=folder/inbox、local_path、mode）。
- `sync_file_index`：本机索引（含 `present_local`）。
- `remote_index`：其他设备广播来的条目（含 `seen_at`，配合设备在线判定决定黄/红）。
- `sync_conflicts`：冲突占位。

## 7. 协议（索引交换，里程碑 3 已实现）

在 ATP v1 之上**向后兼容追加** enum 变体（`aa4c-proto::Message`）：

- `IndexRequest`：发起方握手后请求对端共享索引（里程碑 3 简化：直接请求全量，
  暂不做「先发摘要、按整体哈希判断是否需要全量」的优化——`IndexSummary` 留待规模需要时再加）。
- `IndexEntries { entries, last }`：持有方分批回送条目（rel_path、size、hash），
  `last=true` 标记最后一批（空集合也发一个 `last=true` 空批次表示「无共享」）。
- 完全信任边界在持有方把关：非 `full` 对端一律回空批次，不泄露任何文件名。
- `FetchRequest { rel_path }`（里程碑 4 已实现）：拉取方握手后请求某个共享文件；持有方
  校验完全信任 + 路径在共享范围内后**反转角色**，在同一连接上复用现有 `Offer` + 分块传输
  + `FileAck` 把内容回推，不新增数据通路。拉取方自动接受（是自己要的）。
- 旧版本（v0.1.x）收到这些追加变体会 bincode 解码失败并断开（优雅降级）；按声明顺序
  追加变体不影响既有变体编号。

## 8. 冲突与版本（占位）

元数据优先模式下，同一 `rel_path` 出现不同 `hash`/`mtime` 即记入 `sync_conflicts`，**先人工选择保留哪一份**；自动合并、版本历史留更后续。镜像模式（双向自动覆盖）不在 V0.2 首版。

## 9. UI

见 [UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md)：

- "同步"页 = 跨自己设备的**统一文件视图**，每项带绿/黄/红状态点 + 图例（人话："本地有 / 可下载 / 设备离线"），点黄色即拉取。
- "设置"页 = 已配对设备的**信任层级管理**（朋友 ⇄ 完全信任）。
- 术语合规（§7）：不出现 hash / 索引 / mDNS 等技术词。

## 10. 里程碑切分（建议）

V0.2 较大，建议分阶段（具体里程碑编号待实现计划时定）：

1. ✅ 信任分级：数据模型 + 配对默认朋友 + 设置升级完全信任。
2. ✅ 共享范围 + 本地索引扫描 + Inbox 落点（`aa4c-core/src/sync_index.rs`：定时扫描 +
   传输完成触发扫描，文件监听暂未做，见 §11）。统一文件视图已接真实数据，但仅本机
   索引——黄/红状态要等里程碑 3 才会出现，当前所有条目恒为绿。
3. ✅ 索引摘要交换协议（`IndexRequest`/`IndexEntries`）+ `remote_index` + 统一视图
   （绿/黄/红，只读）：上线即与在线的完全信任设备交换索引摘要（`aa4c-core/src/sync_exchange.rs`），
   落 `remote_index`，`unified::merge` 按限定路径归并本机+远端并着色；完全信任降级即清空
   对端远端索引。拉取触发=启动初拉 + `DeviceFound` 上线触发 + 手动「刷新设备」（见 §11）。
4. ✅ 按需拉取（复用 ATP）：点统一视图里的黄色「可下载」→ Core 从远端索引挑一台**在线
   且完全信任**的持有设备 → A 连 B 发 `FetchRequest{rel_path}` → B 校验完全信任 + 路径在
   共享范围内后**反转角色**用既有发送流回推该文件（`Offer`→分块→`FileDone`/`FileAck`）→
   A 自动接受、落进 Inbox → 本机扫描转绿（黄→绿）。落盘剥掉顶层分组段；Inbox 来源的文件
   会回到同一限定路径而并入同一条目（`aa4c-transfer/src/fetch.rs` + `serve_fetch`）。
5. 冲突标记与人工解决。

## 11. 已确认的设计细节

| 议题 | 决定 | 落点 |
|------|------|------|
| 大目录索引维护 | 文件监听 + 定时扫描兜底，哈希惰性计算 | §3.2 |
| 同名不同内容（多版本） | 加序号区分、绝不自动覆盖，记入冲突 | §3.4 / §8 |
| Inbox 组织 | 按来源设备 + 时间分组（仅视图） | §5 |
| 完全信任降级 | 立即清空该设备的远端索引缓存，已落地文件保留 | §2 |
| 定时扫描间隔 | 默认 300s（`SCAN_INTERVAL`），另在收到任意一次传输完成事件后立即追加一次扫描 | §3.2，`sync_index.rs` |
| 里程碑 2 简化 | 暂未接 `notify` 文件监听，只有定时扫描 + 传输完成触发；Inbox 暂未按来源设备/时间分组，先整体作为一个文件夹展示 | §3.2 / §5 |
| 索引交换触发 | 启动初拉 + `DeviceFound`（设备上线/重连）触发拉取 + 手动「刷新设备」，不订阅频繁的 `DeviceUpdated`；分批每批 ≤1000 条 | §3.3，`sync_exchange.rs` / `dispatch.rs` |
| 里程碑 3 简化 | 统一视图只读（拉取留给里程碑 4）；同路径不同 hash 暂按 rel_path 归并、不拆分版本（冲突拆分留给里程碑 5）；在线判定靠 mDNS 发现快照，CI/无组播环境表现为红 | §3.4 / §8 |
| 按需拉取落点 | 统一拉进 Inbox（缺省接收目录），剥掉顶层分组段后按范围内相对路径落盘——Inbox 来源文件回到同一限定路径并转绿；文件夹来源文件先落 Inbox（呈现为「收到的/…」绿条目，原黄条目仍在），按范围镜像回原结构留作后续细化 | §4，`fetch.rs` |
| 按需拉取边界 | 仅完全信任设备可拉，且只解析本机**已索引（已对外广播）**的条目——绝不按对端任意路径读盘，天然挡掉 `..` 穿越；非 full 回 `Cancel` | §4，`dispatch.rs::FetchServe` |

仍待实现阶段细化：文件监听（`notify` crate）接入时机、移动端后台监听受限的退化方案、Inbox 按来源设备+时间分组的展示、按范围镜像回原目录结构（当前拉取统一落 Inbox）、冲突的版本历史与自动合并策略（V0.2 后）。
