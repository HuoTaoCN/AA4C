# AA4C V0.3 Implementation Plan（AA Connect）

> 依据 [CONNECT_DESIGN.md](CONNECT_DESIGN.md)（设计定稿 v2）与 [PROTOCOL.md](PROTOCOL.md) Part B。
> 给执行 Agent：开工前先读 [AGENTS.md](AGENTS.md)（工程约定）、[HANDOFF.md](HANDOFF.md)（环境坑 + 教训）、CONNECT_DESIGN §12（已确认决策，**不要重开已定案的讨论**）。
> 每个里程碑独立提交、独立验收；提交前自检 `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`（+ 前端改动时 `pnpm build`）。接口变更**先改文档**（PROTOCOL / DATABASE_SCHEMA / API_DESIGN）再写代码。

## V0.3 目标与验收（Definition of Done）

不在同一局域网的两台已配对设备之间：

1. 能建立端到端加密连接（直连或经自建 `aa4c-server` 中继），并跑通 V0.1 发送 + V0.2 同步（索引交换 / 按需拉取）；
2. 断线后重传只补缺口（断点续传）；
3. 能生成/打开分享链接（先局域网范围）；
4. 全程不引入账号体系；远程功能默认关闭，未配置服务器时行为与 V0.2 完全一致。

## 里程碑总览（顺序已定：中继先于打洞）

| 里程碑 | 内容 | 前置 | 交付判定 |
|--------|------|------|----------|
| C1 ✅ | QUIC 会话层 + 断点续传 | — | 两 Core 手填地址走 QUIC 完成传输 + 断传续传 e2e 测试 |
| C2 | `aa4c-server` 信令面 + 客户端接入 | C1 | e2e：两 Core 经 server 相互 Lookup 到端点并建连 |
| C3 | 中继面（Relay）→ **远程可用成立** | C2 | e2e：禁用直连路径，两 Core 经中继完成传输 |
| C4 | 远程同步 / 发送 + 连接质量 UI | C3 | e2e：远程通道跑通索引交换 + 拉取；UI 显示连接方式 |
| C5 | NAT 打洞（提速优化） | C2 | 双真实网络人工验证；失败自动回退中继无感 |
| C6 | 分享链接（可与 C3–C5 并行） | C1 | e2e：生成→打开→按 token 只读取回；过期/吊销被拒 |

里程碑完成后照例更新 CHANGELOG / HANDOFF（进度表 + 下一步）/ 相关设计文档的「已实现」标注。

---

## C1 — QUIC 会话层 + 断点续传（✅ 已完成）

**目标**：广域网传输通道（quinn）+ 证书固定复用 + 单流等价迁移 + `ResumeReport` 断点续传。**不依赖服务器**：测试两端手填地址。

**实现落点与相对设计的修正**（供 C2+ 参考）：
- 索引/拉取的入站分流从「配对/索引共用 `IncomingTlsStream`（TCP 具体类型）」拆成两条：配对仍走
  TCP 具体类型（V0.3 未做远程配对）；索引/拉取改走新增的 `SharedStream = Box<dyn AsyncDuplex>`，
  TCP 与 QUIC 入口（`run_incoming` / `run_incoming_quic`）读完 Hello 后共用同一个 `dispatch_shared`。
- `ResumeReport` 交换是**确定性**的（proto≥3 两端必发/必等），不是步骤 4 原计划里「非阻塞尝试读，
  超时则不续传」——后者有竞态（迟到的 ResumeReport 会被当成下一步的意外消息）。
- `.aa4c-part` 清理策略改了：只有「明确取消」（本地取消/对端主动 Cancel）才清理；网络掉线等
  意外中断**保留** part 文件（续传的前提）。连带发现并修了一个既有小缺口：发送方内部（分块途中）
  检测到取消时原先不通知对端，现改为通知，恢复 PROTOCOL §7 规则 3 的原意。
- QUIC 传输配置加了 keep-alive(2s) + 空闲超时(8s)，而不是照搬 quinn 默认 30s 或干脆关闭——
  两者都会跟「等待用户确认可长达 60s」冲突或让死连接测不出来，取舍见 `quic.rs::transport_config`。
- `rust-version` 从 1.77 升到 1.85（quinn MSRV 要求）；顺带修了一处因 MSRV 提升而变化的 clippy 建议
  （`map_or(true, ..)` → `is_none_or`，`orchestrate.rs`）。

### 前置阅读

- CONNECT_DESIGN §5（会话层决策）、§12（表格里 QUIC / 续传两行）
- PROTOCOL §10（单流等价迁移）、§13（ResumeReport 定稿）、§14（版本协商）
- `crates/aa4c-transfer/src/`：`lib.rs`（服务结构 / `send` / `fetch_file`）、`send.rs`/`recv.rs`（**收发循环已泛型化** `AsyncRead + AsyncWrite + Unpin`，这是单流迁移的基础）、`fetch.rs`
- `crates/aa4c-identity/src/`：`tls_server_config` / `tls_client_config`（证书固定 verifier，QUIC 直接复用）

### 步骤

1. **文档先行**：把 `aa4c-types::PROTO_VERSION` 升 3 的语义写进 PROTOCOL §14（v3 = QUIC 通道 + ResumeReport；TCP 通道上仍可协商 3 但行为同 2——版本号表达能力上限，不强制通道）。确认 §13 `ResumeReport` 字段后在 `aa4c-proto` 末尾**追加**变体（绝不改既有变体；参照 §8b 的追加注释风格）。
2. **依赖**：workspace 加 `quinn`。⚠️ **rustls 版本/加密后端必须与现有 `tokio-rustls` 对齐**（看 `Cargo.lock` 里 rustls 版本与 ring/aws-lc provider；quinn 的 rustls 特性要选到同一套，否则 `ServerConfig`/`ClientConfig` 类型不互通、编译期就会撞墙）。
3. **`aa4c-transfer` 新增 `quic.rs`**：
   - `quic_listen(identity, udp_port) -> (Endpoint, incoming loop)`：復用 `tls_server_config`（rustls config 转 quinn `ServerConfig`）；UDP 端口与 TCP 同号（`listen_port`）。
   - `quic_connect(identity, peer_id, addr) -> bidi stream`：复用 `tls_client_config(Some(peer_id))` 证书固定；ALPN 建议 `b"aa4c"`。
   - **单流适配**：开一条 bidi 流，`tokio::io::join(recv_stream, send_stream)` 得到 `AsyncRead+AsyncWrite`，直接喂给既有 `client_hello`/`server_hello` + 收发循环——**协议层零改动**。
   - 入站分流：QUIC 入站连接读首消息后走与 TCP `run_incoming` 相同的分发（Offer/Index/Fetch/Pair 复用同一套 match；抽公共函数，别复制粘贴）。
4. **断点续传**（两侧都在既有循环里改，量不大）：
   - 接收端 `recv.rs::session`：accept 后、进入 `receive_files` 前，若协商 proto ≥ 3：对每个 `rel_path` 找现存 `.aa4c-part`，按 4 MiB 块重算 BLAKE3 得连续可信前缀 `verified_bytes`，发 `ResumeReport`；`receive_files` 里 `OpenFile` 支持从偏移续写（`.aa4c-part` 以 append 打开 + hasher 需先喂已有前缀——注意 hasher 状态要从头喂已验证字节，或存分块 hash 视实现取舍，选简单的：重新流式读前缀喂 hasher）。
   - 发送端 `send.rs::transfer_files`：`OfferAnswer{accept}` 后若 proto ≥ 3，非阻塞地尝试读 `ResumeReport`（带短超时；收不到=从头传）；对应文件 `send_file` 从 `verified_bytes` 起 seek + Chunk offset 起点非零；progress 初始值计入已传部分。
   - ⚠️ 接收端 hash 校验语义不变：`FileDone` 时的整文件 hash 必须对**全文件**成立。
5. **Core 接线**：`Core::start` 在 TCP 监听旁启动 QUIC 监听（同端口 UDP）；`resolve_peer` 本里程碑不改（地址仍来自 mDNS/落库）；发送/拉取路径加「对端 proto ≥ 3 且地址可 QUIC 时优先 QUIC，失败回退 TCP」的选择（本里程碑可用环境变量/配置强制 QUIC 以便测试，正式选择逻辑 C4 收口）。
6. **测试**（e2e 进 `crates/aa4c-core/tests/core.rs` 风格，参照既有 `spawn_node`）：
   - `quic_roundtrip_transfer`：两 Core loopback，强制 QUIC 路径，发送文件 → 内容/记录断言（对照 `two_cores_pair_then_transfer`）。
   - `quic_resume_after_disconnect`：中型文件（几十 MiB，分块调小）传输中途断开（发送端 cancel 或直接 drop 连接）→ 重新发起 → 断言第二次传输的续传起点非零（可从事件/progress 或接收端日志断言）且最终 hash 校验通过。
   - `aa4c-proto` 单测：`ResumeReport` 编解码 roundtrip + 旧变体编号不受影响（对照既有追加变体测试）。
7. **文档收尾**：PROTOCOL §10/§13 标「已实现」；CHANGELOG Unreleased；HANDOFF 进度表 + 下一步指向 C2。

### 验收清单

- [ ] `cargo test --workspace` 全绿（含新 e2e 两条）
- [ ] fmt / clippy -D warnings 干净；`pnpm build` 不涉前端可跳过
- [ ] 既有 TCP 路径行为无回归（全部旧 e2e 不动照过）
- [ ] `PROTO_VERSION = 3`，与 proto=2 对端互通不回归（版本门槛测试仍过）

### 明确不做（C1 范围外）

服务器 / Lookup / 打洞 / 中继（C2/C3/C5）；多流并行（C5 后优化）；UI 改动（C4）；移动端 QUIC 后台特调。

---

## C2 — `aa4c-server` 信令面 + 客户端接入（✅ 已完成）

- 新 crate `crates/aa4c-server`（bin）：服务器身份复用 `aa4c-identity`（独立数据目录）；TLS 监听 + 帧层 bincode `ServerMessage`（**先在 PROTOCOL 新增 Part C 定稿字段**，enum 独立、`server_proto=1` 起步）。
- 实现：`SrvHello(Ack)`（下发能力+中继端点占位）/ `Challenge`(nonce) / `Register`（端点+proto+允许名单，内存表 + TTL 续约）/ `Lookup`（校验挑战通过 + 在目标名单内）/ `Signal` 盲转发（给目标活跃连接）。全内存态，无持久化。
- 客户端（`aa4c-core` 新模块 `server_link.rs`）：`settings` 加 `server_url` / `enable_remote`（默认 false，见 DATABASE_SCHEMA §4c）；开关开启时维持长连接（断线重连 + 注册续约）；地址解析 `aa4c://host:port#fp` 并 pin 服务器证书指纹（identity 的 pin verifier 泛化复用）。
- 配对流程带上 `server_hint`（`devices` 迁移加列，DATABASE_SCHEMA §4c.0）；`resolve_peer` 增加「mDNS 未命中 → 向对端 server_hint Lookup」一档。
- 交付物含 `Dockerfile` + CI release 产物（Linux 二进制）；`scripts/dev-server.sh` 本地起服。
- e2e：内嵌启动 server（bin 作 lib 暴露 `run()`），两 Core 配对后仅靠 Lookup（禁 mDNS 地址）建连传输；名单外查询被拒测试；解除配对后 Lookup 失效测试。

## C3 — 中继面（Relay）（✅ 已完成）

- server 加 `RelayRequest/Grant`（一次性 token + 短 TTL，进程内表）与中继数据面（`RelayOpen/Data/Close` 盲转发、按 token 对接两条连接；限速配置项）。
- 客户端连接阶梯：公网直连失败 → 经**被叫方** server 申请中继 → 双方 `RelayOpen` 对接 → 在中继流上跑 TLS（端到端证书固定不变）→ 同一套 ATP。
- e2e：强制中继路径完成传输；token 过期/复用被拒。**此里程碑后远程可用成立，可发 preview**。

## C4 — 远程同步 / 发送 + 连接质量

- `sync_exchange` / `fetch_file` / `send_files` 的对端解析统一走「mDNS → last_addr → Lookup → 阶梯」；在线判定 = mDNS 或注册在期（黄色拉取失败给温和提示 + 重试，见 CONNECT_DESIGN §6）。
- 连接质量：`CoreEvent` 加连接方式事件或在任务上带 `via: direct|punch|relay`；设置页「远程连接」区块（服务器地址 + 开关）+ 设备卡片显示方式。UI 术语人话（直连 / 中继（较慢））。

## C5 — NAT 打洞（优化）

- STUN 反射地址探测（来源实现时定：公共 STUN 或 server 兼做）→ `Signal` 交换候选 → 双向同时发包 → quinn 直连升级；失败回退中继需无感。
- CI 无法模拟 NAT：单测覆盖候选交换状态机，真实成功率人工双网络验证。之后视情况做单任务多流优化。

## C6 — 分享链接（可并行）

- 迁移：`shares` + `share_access` 建表（DATABASE_SCHEMA §4c）；`aa4c-proto` 追加 `ShareRequest{token} / ShareList / ...`（Part C 或 §8c 定稿后动手）。
- 服务侧：token 校验（open + 未过期）→ 复用 `resolve_shared` 边界只回已索引内容 → 只读取回（复用 fetch 反转角色路径）；`share_access` 记账。
- 客户端：生成（选共享范围内条目 + 过期时间 → `aa4c://share/<base58>` + 二维码）、管理（列表/吊销/访问记录）、打开（deep-link 注册：桌面三平台 + Android intent——**独立平台工作量，单独排**；首版可先支持「粘贴链接」打开，deep-link 后补）。
- e2e：生成→打开→取回；过期/吊销/token 伪造被拒；非共享范围路径永不可达。

## 风险与注意事项

- **rustls 生态版本对齐**是 C1 最大的编译风险（quinn ↔ tokio-rustls ↔ 现有 identity 代码），先对 `Cargo.lock` 再选版本。
- **bincode enum 只能追加**：所有新消息（ResumeReport / Share*）一律追加在 `Message` 尾部；`ServerMessage` 是新 enum 不受约束但同样从一开始就守追加纪律。
- 服务器是新的暴露面：所有入站消息先过挑战/验签再处理；未认证连接限内存、限速率、快超时。
- 移动端后台长连接受限（iOS 尤甚）：C2 的注册续约在移动端允许「前台才在线」，不追求后台常驻（记入已知限制）。
- 打洞成功率不设 KPI：C5 是优化，中继兜底是可用性承诺。

## 范围外（明确不做，防止蔓延）

官方公益节点 / 多服务器联邦 / 账号体系 / 匿名分享 / 读写分享 / 抗流量分析 / TURN 标准协议兼容 / 浏览器访问分享页（Web 前端）。
