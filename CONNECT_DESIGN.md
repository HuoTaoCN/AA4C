# AA4C 远程连接与分享设计（V0.3）

> 状态：**设计草案**，对应 [ROADMAP.md](ROADMAP.md) V0.3（AA Connect）。本文档是落地前的设计依据，不含实现代码。
> 关联：线路层权威见 [PROTOCOL.md](PROTOCOL.md) Part B（proto v2 广域网）；信任分级见 [PROJECT_VISION.md](PROJECT_VISION.md) §十；表结构见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)；同步复用见 [SYNC_DESIGN.md](SYNC_DESIGN.md)；界面见 [UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md)。

## 1. 背景与目标

V0.1 只在同一局域网内工作，V0.2 把「自己的多台设备」连成统一文件空间——但两者都要求设备在同一个二层网络（mDNS 可达）。V0.3 要**突破局域网**：不在同一网络的已配对设备之间也能直连同步、发送，并支持把文件**分享**给指定好友 / 设备。

四个目标：

1. **远程直连**：优先 P2P 直连（公网可达 / NAT 打洞），不经第三方看内容。
2. **中继兜底**：打洞失败时经 Relay 盲转发加密字节流，保证可达性。
3. **远程能力**：把 V0.2 的跨设备索引交换 + 按需拉取、以及 V0.1 的 AA 发送，跑到广域网链路上（复用 ATP，不新增数据语义）。
4. **分享链接**：生成带权限 / 过期的分享，指定好友或设备可取，不做社区分享。

设计原则延续 [AGENTS.md](AGENTS.md)：稳定 > 功能，简单 > 复杂，默认安全。**不引入账号体系**——设备身份始终是密钥对（V0.1 起）。

### 1.1 基础设施定位：仅自建（本版本已确认）

**V0.3 只设计「自部署 Rendezvous / Relay」，不假设官方公益节点。** 用户自己跑一个信令 + 中继服务（可同机同进程），在设置里填写其地址；参与远程连接的设备指向**同一个** Rendezvous 才能互相找到。官方公益节点、多节点联邦、节点发现等留到更后续（V0.3 之后）再评估。这样的好处：无中心化运维承诺、信任模型简单（你信任你自己的服务器，且端到端加密使其看不到内容）、先把链路跑通。

## 2. 连接阶梯（ICE-like）

设备要与某台**已配对**设备建立连接时，依次尝试，任一成功即停止（呼应 [PROTOCOL.md](PROTOCOL.md) §12）：

| 顺序 | 方式 | 说明 |
|------|------|------|
| 1 | **局域网直连** | mDNS 发现（同 V0.1），同网优先，零依赖、最快 |
| 2 | **公网直连** | 对端 endpoints 里有可达公网地址（有公网 IP / 已做端口映射）时直连 |
| 3 | **UDP 打洞** | 经 Rendezvous 信令交换 STUN 反射地址，双向同时发包打洞，成功后升级 QUIC |
| 4 | **Relay 中继** | 打洞失败：双方各自连自建 Relay，Relay 盲转发端到端加密字节流 |

- 只对 **`trusted=1` 的已配对设备**发起远程连接；陌生设备不进入此流程。
- 每次连接记录「用了哪一档」，UI 以此显示连接质量（直连 / 打洞 / 中继）。
- **优雅降级**：Rendezvous 不可达时，退回纯局域网行为（同 V0.2），不报错阻塞。

## 3. Rendezvous 信令服务（自建）

一个轻量服务（自部署，可与 Relay 同进程），只当「电话簿 + 信令中转」，**不存文件元数据、不存内容、不存社交关系**。

### 3.1 职责

```
POST /register   { device_id, endpoints[], signed_at, signature }   → 注册/续约本设备公网端点
GET  /lookup     { device_id, proof }                               → 查询对端端点 + relay_hint
WS   /signal                                                        → 打洞信令转发（候选地址交换）
```

- **注册**：设备上线后周期性注册自己的候选端点（本地地址、STUN 反射地址）；消息由**设备私钥签名**，服务器验签防止 DeviceId 抢注。`signed_at` 防重放（服务器拒收过期签名）。
- **查询**：只能查询**已配对设备**——请求需携带**配对关系证明**（`proof`：双方在配对时互签的记录，见 §9），服务器据此放行，防止 DeviceId 枚举扫描。
- **信令**：打洞阶段通过 WS 转发 ICE 候选（STUN 探测到的反射地址）；信令消息同样由设备私钥签名，防伪造。

### 3.2 自建约定

- Rendezvous URL 是**每设备配置项**（设置页填写；见 §8 `rendezvous_url`）。自己的多台设备由用户在每台上填**同一个** URL。
- 与好友互联时，Rendezvous 地址可随**配对 / 分享**流程携带（对方拿到后填入），避免手工同步；细节见 §7.3。
- 服务器无状态可水平扩展（端点映射可放内存 + 短 TTL）；单实例即可满足自用规模。

## 4. Relay 中继服务（自建）

打洞失败时的兜底通道。Relay **只盲转发加密字节流**，不解密、不理解 ATP。

```
RelayOpen   { session_token }      // token 由信令阶段协商，Relay 不知道双方身份
RelayData   <opaque bytes>         // 端到端 TLS 之上，Relay 不可解密
RelayClose
```

- `session_token` 由两端在 §3 信令阶段协商生成，Relay 只按 token 把两条连接对接，看不到 device_id。
- 端到端仍是 TLS 1.3 + 证书固定（同 §5），Relay 是纯管道。
- 限速 / 配额由自建节点运营者（即用户自己）配置；中继流量计入「连接质量」显示，UI 提示「通过中继，速度可能较慢」。
- **不承诺抗流量分析**（非目标，记入 [SECURITY.md](SECURITY.md) 威胁模型）。

## 5. 会话层：QUIC

- 广域网传输通道用 **QUIC**（quinn），TLS 1.3 内建，证书固定规则与 V0.1 §2 完全相同（对端证书公钥 BLAKE3 指纹 == 已记录 DeviceId）。
- **单任务多流**：控制消息一条流，每个文件独立流——替代 V0.1 §7 的顺序传输，支持并行与独立重传。
- **v1 TCP 通道保留**作为局域网回退；`Hello.proto` 协商取双方最小值，v2 端连 v1 端自动降级为 v1 TCP 行为（[PROTOCOL.md](PROTOCOL.md) §14）。
- 断点续传（[PROTOCOL.md](PROTOCOL.md) §13）：`Offer` 扩展 `resume: Option<Vec<FileProgress>>`，接收方对 `.aa4c-part` 已落盘部分按块重算哈希回告可信偏移，发送方续传。广域网链路不稳，续传是刚需。

## 6. 远程能力（复用，不新增数据语义）

连接阶梯（§2）建立的是一条**已认证、端到端加密的双向通道**——上层能力直接复用 V0.1/V0.2 的协议：

- **远程同步**：V0.2 的索引摘要交换（`IndexRequest`/`IndexEntries`）+ 按需拉取（`FetchRequest` → 反转角色回推）原样跑在 QUIC 通道上。完全信任设备即使不在同一局域网，也能交换索引、点黄拉取。`remote_index` 的在线判定从「mDNS 30s」扩展为「mDNS 或 Rendezvous 注册在期」。
- **远程发送**：V0.1 的 `Offer`/分块/`FileAck` 跑在 QUIC 上，对端解析地址从「mDNS 快照」扩展为「Rendezvous 查询」。
- **触发**：设备上线后除 mDNS 外也向 Rendezvous 注册；对端不在局域网时，`resolve_peer` 回落到 Rendezvous 查询 + 连接阶梯。

## 7. 分享链接（AA Share）

把文件 / 文件夹分享给**指定好友或设备**（非社区分享，见 [PROJECT_VISION.md](PROJECT_VISION.md) §产品边界）。

### 7.1 模型

- 一个分享 = `{ token, 目标范围(文件/文件夹), 权限(只读/读写), 过期时间, 访问记录 }`，落 `shares` 表（§8）。
- `token` 是高熵随机串，**即能力（capability）**：持有有效 token 即可按其权限访问，不需要账号。
- 权限：**只读**（下载）/**读写**（上传回来，V0.3 首版可先只读，读写留余量）。
- 过期：绝对时间；过期即失效（服务端拒绝）。可手动吊销（删除 / 置 revoked）。

### 7.2 分享的呈现

- **分享链接**编码 `{ host device_id, share token, rendezvous 提示 }`（不含内容、不含密钥）。
- 打开方式：AA 客户端识别链接 → 经 Rendezvous 解析 host → 走连接阶梯建连 → 出示 token → 按权限取文件。
- 局域网内的分享可不依赖 Rendezvous（直接 mDNS 找到 host）——**分享链接功能可先在局域网落地**，远程可达随连接阶梯就绪后自然生效。

### 7.3 与配对 / 信任的关系

- 分享给**已配对好友**：走既有信任，token 只是「这次给你看这些文件」的范围限定。
- 分享给**尚未配对**的人：token 本身作为一次性凭据；首次访问可触发一次轻配对（或纯 token 匿名只读，二选一，实现前定）。V0.3 首版建议**仅对已配对好友分享**，把匿名分享留到有把握时再开。
- Rendezvous 地址随分享链接携带（§3.2），对方无需事先知道你的服务器。

## 8. 数据模型

落地表见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)（V0.3 段，待建表）。要点：

- `shares`：分享记录（`token` / 目标 rel_path / 权限 / 过期 / 创建时间 / 状态 open|revoked）。
- `share_access`（可选）：访问记录（share_id / 访问方 device_id 或匿名 / 时间 / 动作），供「查看访问记录」。
- 连接配置存 `settings`（KV，复用现有表）：`rendezvous_url`、`relay_url`（缺省空=纯局域网）、`enable_remote`（总开关）。
- `devices` 可增列或旁表存对端的 Rendezvous 提示 / 最近公网端点（便于回落查询）——具体建表时定，先不过度设计。

## 9. 安全考量（V0.3 新增面）

| 威胁 | 对策 |
|------|------|
| Rendezvous 被攻破 / 作恶 | 只存端点映射；注册验签；端到端加密使其读不到内容；自建=你自己的信任域 |
| DeviceId 枚举扫描 | lookup 需**配对关系证明**（双方配对时互签记录）+ 速率限制 |
| 打洞信令伪造 | 信令消息由设备私钥签名 |
| Relay 作恶 | 只见 opaque 字节 + session_token，见不到 device_id；端到端 TLS 不可解密 |
| 分享 token 泄露 | 高熵 + 可过期 + 可吊销 + 只读默认 + 访问记录可审计；链接不含密钥/内容 |
| Relay 流量分析 | 不承诺抵抗（非目标）；记入 [SECURITY.md](SECURITY.md) |

**配对关系证明（proof）**：配对成功时（[PROTOCOL.md](PROTOCOL.md) §6），双方各自用私钥对「对方公钥 + 时间」签名并交换、落库；日后向 Rendezvous 查询对端时出示，证明「我俩确实配过对」，服务器验签放行。这把「谁能查谁的端点」收敛到已配对关系，杜绝陌生人探测。

## 10. UI

见 [UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md)：

- **设置页**新增「远程连接」：填 Rendezvous / Relay 地址、总开关；显示当前对端连接方式（直连 / 打洞 / 中继）与质量提示。
- **分享页（Share）**从「建设中」转为可用：生成分享（选文件/文件夹 + 权限 + 过期）、管理分享（列表 / 吊销）、查看访问记录。
- 术语合规（[UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md) §7）：不出现 NAT / STUN / Relay 等技术词，用「远程连接 / 直连 / 中继（较慢）」这类人话。

## 11. 里程碑切分（建议）

V0.3 较大，建议分阶段（具体里程碑编号待实现计划时定）：

1. **QUIC 会话层**：广域网通道升 QUIC（quinn）+ 单任务多流 + 证书固定复用；`Hello.proto` 协商与 v1 TCP 回退；不涉信令，先在两端已知地址间跑通 QUIC 传输 + 断点续传。
2. **自建 Rendezvous 信令**：注册 / 查询（带配对证明）/ 信令 三接口 + 客户端接入（上线注册、`resolve_peer` 回落查询）；配对时互签 proof 并落库。
3. **NAT 打洞**：STUN 反射地址探测 + 经信令交换候选 + 双向打洞 → QUIC 升级；连接阶梯 §2 第 1–3 档打通。
4. **自建 Relay 中继**：打洞失败回退，`RelayOpen/Data/Close` 盲转发；连接阶梯第 4 档 + 连接质量显示。
5. **远程同步 / 发送**：把 V0.2 索引交换 + 按需拉取、V0.1 AA 发送跑到远程通道；在线判定并入 Rendezvous。
6. **分享链接**：`shares` 表 + 生成 / 管理 / 访问记录 + token 鉴权 + 只读 + 过期 + 吊销；先局域网落地，远程随链路自然生效。

> 里程碑 1 可先独立于信令交付（两端手填地址即可验证 QUIC + 续传），把风险最大的传输层单独跑通；信令 / 打洞 / 中继再逐档叠加。分享链接相对独立，可与传输层并行。

## 12. 已确认的设计细节

| 议题 | 决定 | 落点 |
|------|------|------|
| 基础设施 | **仅自建 Rendezvous/Relay**，不假设官方公益节点（本版本确认） | §1.1 |
| 账号体系 | 不引入，设备身份始终是密钥对 | §1 |
| 广域网会话层 | QUIC（quinn），证书固定规则同 V0.1，单任务多流 | §5 |
| v1 兼容 | `Hello.proto` 取最小值，v2↔v1 自动降级 v1 TCP | §5 |
| 断点续传 | `Offer.resume` 按块重算哈希回告可信偏移 | §5 |
| 谁能查端点 | 仅已配对设备，凭配对时互签的 proof | §3.1 / §9 |
| 中继信任 | Relay 只见 opaque 字节 + session_token，端到端 TLS 不可解密 | §4 |
| 分享范围 | 指定好友 / 设备，非社区；V0.3 首版建议仅对已配对好友、默认只读 | §7 |
| 能力复用 | 远程同步 / 发送直接复用 V0.2 索引交换 + V0.1 ATP，不新增数据语义 | §6 |

仍待实现阶段细化：STUN 服务器来源（自建 / 复用公共 STUN）、Rendezvous 端点 TTL 与续约间隔、打洞成功率兜底策略、读写分享的冲突与配额、匿名（未配对）分享是否开放及其鉴权、iOS 后台长连接受限下的远程可达退化方案。
