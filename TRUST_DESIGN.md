# AA4C 设备信任传递与跨网可达设计（V0.7 设计稿）

> 状态：**设计稿，未实现**。本文回答一个具体问题——「我的手机、家里的电脑、单位的电脑，怎么在不同网络下连成一个文件空间，且不需要注册任何账号」。
> 关联：连接阶梯与自建服务器见 [CONNECT_DESIGN.md](CONNECT_DESIGN.md)；信任分级的权威定义见 [SYNC_DESIGN.md](SYNC_DESIGN.md) §2 与 [PROJECT_VISION.md](PROJECT_VISION.md) §十；线路协议见 [PROTOCOL.md](PROTOCOL.md)；表结构见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)。

## 1. 背景与目标

V0.3（里程碑 C1–C6）已经打通「跨网络连接」的技术路径：连接阶梯（局域网直连 → 公网直连 → UDP 打洞 → 中继兜底）+ 自建 `aa4c-server`。但它留下两个**使用层面**的门槛：

1. **必须自建服务器**。CONNECT_DESIGN.md §1.1 明确决定「仅自建，不假设官方公益节点」。对「我就想让我的两台电脑互通」的用户，要先有一台 VPS，门槛过高。
2. **信任只能当面建立**。配对要走 PIN，实际上要求两台设备同处一个局域网。于是「家里的电脑」和「单位的电脑」永远配不上对——它们从来不在同一个网络里，而你不可能把台式机搬来搬去。

第 2 条才是真正的死结：**不解决它，第 1 条做得再好也没用**——两台设备连得上，但互相不认识。

本设计的目标：

1. **信任可传递**：在同一局域网当面建立的信任，能安全地扩展到「我的其它设备」，不需要每两台设备都当面配一次。
2. **不引入账号**：设备身份始终是密钥对（V0.1 起的既定原则，CONNECT_DESIGN.md §1 复述过）。
3. **降低基础设施门槛**：把「必须有 VPS」降到「家里有台常开的设备」，并让直连在更多真实网络下成立。
4. **不做虚拟网卡**：见 §2.3。

## 2. 先厘清三个前提

调研 Tailscale / ZeroTier / EasyTier / Syncthing 之后，有三个常见认知需要先纠正，否则会把方案带偏。

### 2.1 Tailscale / ZeroTier 其实都要账号

- **Tailscale** 必须通过身份提供商（Google / GitHub / Microsoft 等）登录，控制面（coordination server）在厂商手里。自建要换成 Headscale，那是另一套部署。
- **ZeroTier** 要一个 network ID，来自 my.zerotier.com（要注册）或你自建的 controller。

**真正无账号的是 EasyTier 与 Syncthing 那一类**：身份不是账号，而是密钥 / 共享密钥。EasyTier 用 `--network-name` + `--network-secret` 作为网络标识，无中心认证；Syncthing 用设备证书，设备 ID 就是证书指纹，公共发现服务器不需要任何注册。

**结论**：可以做到无账号，但要照 Syncthing / EasyTier 的路子走，不是照 Tailscale 的。

### 2.2 「零第三方」做不到 NAT 穿透

这是原理性的，不是实现问题：两台在各自 NAT 后面的机器，必须有个**汇合点（rendezvous）**来（a）互相告知当前公网地址、（b）协调同时发包打洞。区别只在汇合点是谁：

| 汇合点 | 要账号 | AA4C 现状 |
|---|---|---|
| 你自建的 `aa4c-server` | 否 | ✅ 已实现（C2–C5） |
| 公共共享节点（EasyTier 式） | 否 | ❌ |
| 公共发现 + 中继池（Syncthing 式） | 否 | ❌，CONNECT_DESIGN §1.1 明确推迟 |
| 全网 DHT | 否 | ❌ |
| 厂商控制面（Tailscale / ZeroTier） | **是** | 不适用 |

**结论**：目标应当是「**不需要第三方服务商**」，而不是「不需要任何服务端」。最现实的形态是**你自己家里那台常开的设备兼任汇合点**（§6.3）。

### 2.3 我们不需要照搬 EasyTier

EasyTier / Tailscale / ZeroTier 都是**建虚拟网卡的 VPN**——要虚拟 IP、路由表、子网代理、跨平台 TUN 驱动。AA4C 只需要「找到对方 + 打通一条应用层连接」，而这套东西 V0.3 已经有了（连接阶梯 + QUIC + 端到端 mTLS）。

把整套组网搬进来是杀鸡用牛刀，而且会引入 TUN 驱动、管理员权限、路由冲突这些与项目「简单 > 复杂」原则直接冲突的东西。**值得借鉴的是它们的去中心汇合思路，不是它们的组网实现。**

## 3. 现状盘点

### 3.1 已经有的

- 连接阶梯四档（CONNECT_DESIGN §2），含中继兜底与 UDP 打洞，已实现并有 e2e 覆盖。
- 设备身份 = Ed25519 密钥对 + 自签证书，**设备 ID 就是证书指纹**（`device_id_from_cert`）。这与 Syncthing 的模型同构，是本设计能成立的基础。
- 信任分级：`devices.trust_level` ∈ `full | friend`；**只有 `full` 之间交换文件索引**（SYNC_DESIGN §2），这是既有的隐私边界。
- `PairServerHint`（PROTOCOL §17）：配对确认后、写库之前，双方交换各自的 home server 地址。**这是本设计要复用的协议扩展先例**。

### 3.2 一个实际的缺口：IPv6 全链路没打通

翻代码核实（不是推测）：

| 位置 | 现状 |
|---|---|
| `crates/aa4c-transfer/src/quic.rs:128` | `SocketAddr::from(([0, 0, 0, 0], port))` —— QUIC 只绑 IPv4 |
| `crates/aa4c-transfer/src/lib.rs:1026`、`:1031` | `TcpListener::bind(("0.0.0.0", …))` —— TCP 只绑 IPv4 |
| `crates/aa4c-server/src/reflect.rs:34` | 同上，反射端点只绑 IPv4 |
| `crates/aa4c-discovery/src/parse.rs` `pick_addr` | **只给 IPv4 打分**；IPv6 仅在「一个 IPv4 都没有」时才被退而求其次地选中 |

也就是说：**AA4C 目前收不了、也基本不会选 IPv6 连接。**

这对本设计的目标场景影响很大——国内三大运营商的家宽现在普遍给**每台设备**下发公网 IPv6，而 IPv4 在 CGNAT 后面。两端都有全局 IPv6 时：

- 走 IPv4：双方都在 CGNAT 后 → 打洞经常失败 → 被迫走中继（慢，且要服务器带宽）
- 走 IPv6：双方都是全球可路由地址 → **根本不需要穿透，直连即可**

**但要说清楚一个常见误解**：IPv6 解决的是「**能不能连**」，不解决「**知不知道对方地址**」。IPv6 前缀会随拨号变化，仍然需要一个发现机制告诉你对端当前地址。所以 IPv6 **不能**替掉汇合点，它替掉的是**打洞和中继**。

## 4. 问题拆解

必须把两件事分开，它们互相独立、可以分别推进：

- **问题 A — 连通性**：不同网络的两台设备怎么连上（§6）
- **问题 B — 信任传递**：家里的电脑怎么认识单位的电脑（§5）

**B 不依赖 A**：先把信任关系建好，连通性用现有的中继兜底就能跑通。反过来不成立——连得上但互不认识，毫无意义。所以**B 优先**。

## 5. 问题 B：信任传递

### 5.1 目标场景

```
第 1 步（在家，同一局域网）    手机 P  ←—— PIN 配对 ——→  家里电脑 H     双方 full
第 2 步（在单位，同一局域网）  手机 P  ←—— PIN 配对 ——→  单位电脑 W     双方 full
期望结果                       家里电脑 H  ←— 自动/半自动 —→  单位电脑 W
```

H 与 W 从未同处一个网络，永远走不了 PIN 配对。手机 P 是它们唯一的共同熟人。

### 5.2 三个候选方案

**（A）自动引荐（Syncthing 的 introducer）**
完全信任设备之间交换「我信任谁」的列表，收到即**自动**加入信任。

Syncthing 官方文档自己列了两个坑：
- 传递会失控：*"An introducers' introducer will become your introducer as well."*
- **删不掉设备**：手动删除一个被引荐的设备后，「只要引荐者仍被标记为 introducer，它就会被自动重新加回来」；互为 introducer 时两边会不停地互相「重新引荐」已删除的设备。

**（B）个人设备组 + 组 CA**
建组时生成组根密钥，每台入组设备拿一张组根签发的证书；任意两台凭证书直接互认，不需要引荐者在线。

密码学上最干净，但要引入：根密钥保管与备份、证书签发、有效期、**撤销**。而撤销在没有中心控制面时很难做对——离线设备收不到撤销列表，存在窗口期。根密钥若复制到每台设备，则「移除某台设备」形同虚设（它自己就能重新签发）。

**（C）引荐 + 一次确认**
手机通过已有的完全信任通道告诉家里电脑：「W 也是你的设备，指纹 = …」。家里电脑**不自动信任**，而是在界面上提示，由用户点一下确认。

### 5.3 对比与选择

| | (A) 自动引荐 | (B) 组 CA | **(C) 引荐 + 确认** |
|---|---|---|---|
| 改动量 | 小 | **大** | 小 |
| 引荐者需在线 | 是（首次） | 否 | 是（首次） |
| 传递失控风险 | **高** | 无 | 无（每步有人确认） |
| 「删了又被加回来」 | **会** | 不适用 | 不会（§5.7） |
| 撤销语义 | 模糊 | **难（无中心）** | 清晰（本机删一行） |
| 根密钥保管问题 | 无 | **有** | 无 |
| 用户操作 | 零 | 零 | **点一次** |

**选定 (C)**。理由：

1. **它避开了 (A) 两个已被真实用户证实的坑**——传递失控与删不掉，Syncthing 文档自己在警告。
2. **它不引入 (B) 的 CA 复杂度**，尤其是无中心撤销这个已知难题。
3. **代价只是点一下，而那一下恰好是正确的时机**——「确认这确实是我的设备」本来就该由人来判断一次。
4. **契合项目一贯风格**：归档是「规则自动、AI 建议」，配对是 PIN 确认，AI 输出永远只进「待确认」队列。自动信任一个从未谋面的指纹，与这条主线不一致。

**(C) 可以平滑升级到 (B)**：引荐通道与数据模型不变，只是把「用户确认」换成「组根签名自动验证」。所以现在选 (C) 不会堵死将来的路。

### 5.4 为什么 (C) 在密码学上是干净的，而不是将就

关键在于 AA4C 的信任锚点**本来就是证书指纹**：

- 设备 ID = `BLAKE3(公钥)`，连接时校验对端证书指纹与已知 ID 一致（证书固定，V0.1 起）。
- 引荐传递的正是这个指纹。**拿到正确的指纹 = 拿到完整的信任锚点**，后续连接仍是 mTLS + 指纹固定，**不存在 TOFU 窗口**。
- 传递通道（P ↔ H）本身已经是 mTLS + 完全信任，是可信信道。

所以 PIN 那一步的作用——「在可信信道上安全地交换指纹」——被引荐通道等价地满足了。**PIN 不是信任的来源，指纹才是**；PIN 只是当年没有可信信道时用来搬运指纹的手段。这也是为什么被引荐的设备不需要再走一次 PIN。

### 5.5 协议设计

照 `PairServerHint`（PROTOCOL §17）的既有先例：**不修改任何已有消息结构体**（`Message` 是 bincode 位置编码，给既有变体追加字段会破坏所有旧客户端的解码），而是**追加新变体 + proto 版本 gate**。

```rust
/// 引荐：我还完全信任这些设备（PROTOCOL §18）。
IntroduceRequest,
IntroducePeers {
    peers: Vec<PeerIntro>,
},

pub struct PeerIntro {
    /// 设备指纹 = 信任锚点，收方据此即可安全建立连接。
    pub device_id: String,
    /// 仅供界面显示，收方可自行改名。
    pub name: String,
    pub platform: Platform,
    /// 对端的 home server 地址，供跨网寻址（同 PairServerHint 的语义）。
    pub server_hint: Option<String>,
}
```

- 新增 `INTRODUCE_PROTO_VERSION: u16 = 6`，`PROTO_VERSION` 提升到 `6`。proto < 6 的一方不认识这两条消息，两端都不发送，行为与旧版完全一致（同 `RESUME_PROTO_VERSION` / `SHARE_PROTO_VERSION` 的 gate 惯例）。
- **交换时机**：并入既有的 `sync_exchange` 周期（跨设备索引交换，30s 定时器 + `DeviceFound` 即时触发）。不并进配对流程——配对只发生一次，之后新加入的设备就传不出去了。
- **只在 `full` ↔ `full` 之间交换**，与文件索引同一道闸（SYNC_DESIGN §2：「只有完全信任设备之间才交换文件索引」）。
- **只引荐 `full` 设备，不引荐 `friend`**。语义是「这些都是我的设备」；把朋友也广播出去等于泄露社交关系图，是不同性质的事。

### 5.6 数据模型

不新建表，`devices` 加两列（都是 `ALTER TABLE ADD COLUMN`，**不触发建表重建**——迁移 012 的教训见 DATABASE_SCHEMA §2.2）：

```sql
ALTER TABLE devices ADD COLUMN introduced_by TEXT;              -- 谁引荐的；NULL = 不是引荐来的
ALTER TABLE devices ADD COLUMN introduce_dismissed INTEGER NOT NULL DEFAULT 0;
```

- 被引荐但尚未确认的设备：`trusted = 0`、`trust_level` 为空、`introduced_by = <引荐者 id>`。名字、平台、`server_hint` 复用既有列。
- 界面列出 `introduced_by IS NOT NULL AND trusted = 0 AND introduce_dismissed = 0` 的设备作为「待确认」。
- 用户确认 → 走既有的信任升级路径（`trusted = 1`、`trust_level = 'full'`）。
- 用户忽略 → `introduce_dismissed = 1`，后续同一引荐不再打扰。

### 5.7 撤销与降级语义（(C) 相对 (A) 的关键优势）

- **本机移除**：把 W 降级或解除配对，就是本机改一行，立即生效。W 在本机的远端索引按既有规则清空（SYNC_DESIGN §2「降级即清理」）。
- **会不会被加回来？** 下一轮引荐里 W 仍会出现，但**只会变成一条「待确认」提示，不会自动恢复信任**。若用户当初点的是「忽略」，`introduce_dismissed` 会让它连提示都不再出现。**这就是 (A) 的「删了又被加回来」在 (C) 里不成立的原因。**
- **引荐者被移除**：不影响已经确认过的设备。信任一旦由用户确认，就是本机与对方之间的直接关系，不再挂靠引荐者——这与 Syncthing 的 `introducedBy` 级联移除**刻意不同**，因为在「都是我的设备」这个语义下，手机丢了不该导致家里和单位的电脑互相不认识。

### 5.8 UI

设置页「已配对设备」上方新增「待确认的设备」区块：

```
┌────────────────────────────────────────────────┐
│ 待确认的设备                                    │
│                                                │
│ 💻 单位电脑                                     │
│    你的「手机」说这也是你的设备                  │
│    [ 标记为我的设备 ]  [ 忽略 ]                 │
└────────────────────────────────────────────────┘
```

文案要说清「谁说的」——这是用户判断要不要信的唯一依据。不出现指纹全文（术语），但提供展开查看（排查用）。

### 5.9 安全边界

| 议题 | 说明 |
|---|---|
| 引荐 ≠ 信任 | 引荐只产生一条「待确认」记录，必须用户点确认才升级。这是与 (A) 的本质区别，也是最后一道防线。 |
| 传播范围 | 只在 `full ↔ full` 的 mTLS 通道上传播，且只传 `full` 设备。不泄露 `friend`（社交关系）。 |
| 一台完全信任设备被攻破 | 攻击者能做的是「建议你信任某个陌生指纹」。用户确认这一步挡住了自动扩散。注意：攻破一台 `full` 设备本来就能读到全部同步文件，所以引荐带来的**增量**风险仅限于「诱导你信任第三方设备」，而这一步有人工确认。 |
| 无 TOFU 窗口 | 引荐传的是指纹本身，后续连接仍走证书固定校验，与当面配对得到的安全性等价（§5.4）。 |
| 拒绝服务 | 恶意 `full` 设备可以刷大量引荐。缓解：单次引荐条数上限、待确认列表容量上限，超出直接丢弃。 |

## 6. 问题 A：连通性增强

以下都是对 CONNECT_DESIGN §2 连接阶梯的**增强**，不替代它。

### 6.1 IPv6 双栈（优先级最高）

改动集中、纯收益、不碰架构，且不需要任何第三方：

1. 三处监听改为绑 `[::]` 双栈（`quic.rs:128`、`lib.rs:1026/1031`、`server/reflect.rs:34`）。**注意**：Windows 需显式关闭 `IPV6_V6ONLY`；Linux 行为取决于 `net.ipv6.bindv6only`，不能依赖默认值，要显式设置。
2. `discovery/parse.rs` 的 `pick_addr` 增加 IPv6 打分：**全局单播 IPv6 应当优先于 CGNAT IPv4**（`100.64.0.0/10`），因为后者几乎必然要打洞或中继。同时排除链路本地 `fe80::/10`（除非带 scope id）与 ULA 的跨网使用。
3. 上报 endpoints 时同时带上 IPv6，供公网直连档（第 2 档）使用。

**预期收益**：家宽 ↔ 蜂窝网这类场景很可能直接落到第 2 档（公网直连），跳过打洞与中继。

**明确不做的承诺**：IPv6 不能替掉汇合点（§3.2），它替掉的是打洞和中继。

### 6.2 UPnP / NAT-PMP 自动端口映射

目前只有 Transmission 在给 BT 端口做映射（`port-forwarding-enabled`），**AA4C 自己的传输端口没有做**。补上之后，IPv4 家宽（有公网 IP 的那部分）也能被直连命中第 2 档。

尊重既有的「默认安全」原则：应当是**可关闭**的，且默认行为要在设置页说清楚（它会在你的路由器上开一个端口）。

### 6.3 让家里那台常开的设备兼任汇合点

现在 `aa4c-server` 是独立二进制，实际要求用户有 VPS。可以让桌面端内置一个**可选的 server 模式**：家里的台式机/NAS 开着，它就是你的汇合点。

- 门槛从「要有 VPS」降到「家里有台常开设备」。
- 配合 §6.1，这台设备有公网 IPv6 时无需端口映射即可被直连。
- 代码复用：`crates/aa4c-server` 已存在，桌面端把它作为库依赖起一个进程内实例即可。

**必须诚实说明的前提**：你仍然需要一个**稳定入口**才能找到这台设备——要么 DDNS，要么相对固定的地址。这是「零第三方」的真实边界：不需要服务商，但需要你自己有个能被找到的落脚点。

### 6.4 明确不做

| 不做 | 理由 |
|---|---|
| 虚拟网卡 / TUN 组网 | §2.3：我们只要一条应用层连接，不要 VPN。会引入驱动、权限、路由冲突。 |
| 公共发现 / 中继节点池 | 要有人长期运营；且中继虽是盲转发（内容端到端加密），**元数据仍可见**（谁在什么时候连了谁）。与「仅自建」的既定定位冲突，留到之后单独评估。 |
| 全网 DHT | 收敛慢、可靠性差；虽然我们已打包 Transmission（自带 DHT 栈），但借它的 DHT 做设备发现是 hack，语义不清。自研 DHT 是独立的大工程。 |
| 账号体系 | V0.1 起的既定原则，本设计不动摇。 |

## 7. 里程碑切分

| 里程碑 | 内容 | 依赖 |
|---|---|---|
| **R1** | IPv6 双栈（§6.1）——三处 bind + `pick_addr` 打分 + endpoints 上报——**已实现**，落地细节见 §7.2 | 无 |
| **R2** | 信任引荐协议（§5.5）+ 数据模型（§5.6）+ 待确认 UI（§5.8）——**已实现**，实现偏差见 §7.1 | 无（与 R1 独立） |
| **R3** | UPnP / NAT-PMP（§6.2）——**已实现（仅 UPnP IGD）**，落地细节与取舍见 §7.3 | R1 |
| **R4** | 桌面端内置 server 模式（§6.3） | R1 |

R1 与 R2 可并行；**R2 是解决用户核心痛点的那一个**（H 与 W 互相认识），R1 是让连接更可能直连的那一个。

### 7.1 R2 实现与本设计稿的偏差

落地时改了四处，都是设计稿写的时候没查到底的细节：

| 处 | 设计稿 | 实现 | 为什么 |
|---|---|---|---|
| `PeerIntro` 字段 | 只有 `device_id` | 增加 `public_key: Vec<u8>` | 两个理由。①`devices.public_key` 是 `NOT NULL`，光有指纹落不了库；②`device_id == BLAKE3(public_key)`，收方**本地就能校验两者自洽**，恶意引荐者递不出一个与公钥对不上的指纹。这是白捡的一层完整性校验，设计稿漏了 |
| `IntroducePeers` | `{ peers }` | `{ peers, last }` | 与 `IndexEntries` 同形分批。条数本来极小，分批只是留扩展余量；`last` 也让空列表有个明确的收尾帧，请求方不用等超时 |
| 交换时机 | 并入 `sync_exchange` 的 30s 周期 | **独立的 5 分钟周期**（`introduce.rs`） | 索引 30s 一轮是因为文件随时在变；「我有哪几台设备」几乎不变，为它每 30s 给每台完全信任设备多开一条 TCP+TLS 连接是纯浪费。而用户从「在单位配对完手机」到「走回家点确认」中间隔的时间远不止 5 分钟，压到秒级没有任何收益 |
| 待确认记录的 `trust_level` | 「为空」 | 走列默认值 `'friend'` | `trust_level` 是 `NOT NULL DEFAULT 'friend'`（迁移 002），根本不允许为空。`trusted = 0` 已经关掉了一切能力，这一列在待确认状态下是惰性的，确认时才置 `'full'` |

### 7.2 R1 落地细节

按 §6.1 列的三处 bind 全部改成双栈，另外补了两处那份清单没点到的：

| 处 | 改动 |
|---|---|
| 传输层 TCP（`bind_with_fallback`） | 绑 `[::]`，IPv6 不可用时自动回落纯 IPv4 |
| 传输层 QUIC（`quic::listen`） | 同上。**改用 `quinn::Endpoint::new` 接管自建 socket**，而不是 `Endpoint::server`——只有自己建 socket 才能显式设 `IPV6_V6ONLY` |
| 服务器反射面（`server/reflect.rs`） | 同上。反射服务尤其需要：它的职责就是告诉对端「我看到你的地址是什么」，只绑 IPv4 的话 IPv6 客户端根本问不到自己的地址 |
| **（补）自建服务器 TCP 监听** | `AA4C_SERVER_LISTEN` 默认从 `0.0.0.0:42420` 改成 `[::]:42420`。只监听 IPv4 的服务器会把 R1 的收益吃掉一半——手机在 IPv6-only 蜂窝网里够不着它。管理员显式指定了具体地址时按原样绑，不改写意图 |
| **（补）`primary_local_ip`** | 拆成 v4/v6 两个探测，候选端点同时上报两族地址。只报 IPv4 的话，即便双方都有公网 IPv6，对端也永远拿不到那个能直连的地址 |

两个必须写死、不能赌默认值的点：

- **`IPV6_V6ONLY` 必须显式关闭**。Windows 默认是**开**的（那样 IPv4 会整个失联）；Linux 取决于 `net.ipv6.bindv6only` 这个 sysctl；OpenBSD 则根本不允许关——所以那一步失败时要**回落纯 IPv4**，而不是启动失败。
- **IPv4 映射地址（`::ffff:a.b.c.d`）要在拿到的第一时间还原**。双栈监听之后，一条普通 IPv4 入站连接的 `peer_addr()` 会以映射形式出现，而这个值会一路写进 `devices.last_addr`、以及**打洞候选**里。不还原的话：同一台设备在打通双栈前后会存出两种写法；候选列表里 `1.2.3.4:x` 和 `::ffff:1.2.3.4:x` 去重失效，白白多打几轮探测。落到 `aa4c_proto::net::normalize_mapped`，在四个取地址的位置各调一次。

`pick_addr` 的排序改成 IPv4 / IPv6 同尺比较：私有 IPv4 与 IPv6 ULA 同为第 0 档（同网直连最快）、**全局单播 IPv6 第 1 档**、其他可路由 IPv4 第 2 档。全局 IPv6 排在普通公网 IPv4 之前，是因为前者跨网依然有效；而 CGNAT 段（`100.64.0.0/10`）本来就在拒绝列表里。链路本地 `fe80::/10` 一律排除——mDNS 记录里带不出 scope id，拿到也连不上。

§9 的两个待确认问题也在实现中定了：**只带 `server_hint`、不带 `last_addr`**（不把内网地址暴露
给未确认的设备）；**多跳会传递，但每一跳都要用户确认一次**，且未确认的记录不参与转发。

### 7.3 R3 落地细节

#### 只做了 UPnP IGD，NAT-PMP / PCP 明确后置

不是漏了，是查过之后的取舍：`igd-next` 用 SSDP 组播**自己发现网关**；而 NAT-PMP/PCP 的
客户端库（`crab_nat` 是目前最完整的一个）一律要求调用方**自己传网关 IP**，它本身不做发现。
补上就得再引一个读系统路由表的平台相关依赖。收益不对称——家用路由器绝大多数支持 UPnP IGD，
NAT-PMP/PCP 的主要地盘是已停产的 AirPort 和部分 OpenWrt/pfSense。留给后续里程碑。

#### 两层闸

| 闸 | 默认 | 作用 |
|---|---|---|
| `enable_remote` | **关** | 「不配置、不打开就完全不出网」这条默认安全的姿态由它保证。关着时端口映射一个包都不发 |
| `enable_port_mapping` | **开** | 用户既然主动打开了远程连接，要的就是「能被连上」，再要求他找第二个开关才肯打洞是反的（同 Transmission 自己 `port-forwarding-enabled: true` 的默认）。留这个开关是因为它确实会在用户路由器上开一个端口，界面上写明了 |

#### 几个不显然的决定

- **TCP 和 UDP 都要映射**：AA4C 的 QUIC 与 TCP **共用同一个端口号**，只映射一条等于砍掉
  一半传输能力。而且 **UDP 那条失败要把 TCP 那条撤回**——只有 TCP 通、QUIC 不通的半吊子
  状态比干脆不映射更难排查，还会让对端拿到一个 UDP 打不通的候选，白白拖慢连接阶梯。
- **申请带租约的映射（1 小时），不申请永久映射**：永久映射在程序崩溃 / 断电后会**永远留在
  路由器上**，没人来收。正常路径是停机时主动拆（`Core::shutdown` 的停机信号一到就拆），
  租约是异常路径的兜底。续约在租约过半时做，并对「网关给了个荒唐的短租约」设了 30 秒下限。
- **映射到的地址必须进候选端点**：这是 R3 有没有用的分界线——在路由器上开了洞却不把那个
  地址告诉对端，对端还是只能打洞或走中继，映射等于白做。接进 `local_candidate_endpoints`，
  注册、打洞回信、打洞发起三条路径都会带上它。
- **网络层放在 `PortMapper` trait 后面**：真实实现会去改用户的路由器，测试必须能换成假的。
  同 `RelayDialer` / `PunchDialer` / `SidecarSpawner` 的既有注入惯例。另加
  `CoreConfig::disable_port_mapping` 测试开关（同 `disable_discovery`）——集成测试里有几个
  用例会打开 `enable_remote`，**不能靠「反正测试跑得比退避周期快」这种时序巧合**来保证不去
  碰跑测试那台机器的路由器。

## 8. 已确认的设计细节

| 议题 | 决定 | 落点 |
|---|---|---|
| 信任传递模型 | **引荐 + 一次用户确认**，不自动信任 | §5.3 |
| 为何不自动 | Syncthing 官方文档记录的两个真实坑：传递失控、删了又被加回来 | §5.2 |
| 为何不做组 CA | 无中心撤销是已知难题；根密钥保管与备份是新的一整类问题。(C) 可平滑升级到 (B)，不堵路 | §5.2 / §5.3 |
| 引荐的安全性依据 | 信任锚点本来就是证书指纹；引荐传的就是指纹，无 TOFU 窗口。PIN 不是信任来源，只是搬运指纹的手段 | §5.4 |
| 协议扩展方式 | 追加 `Message` 变体 + `INTRODUCE_PROTO_VERSION = 6` gate，**不改既有变体**（bincode 位置编码） | §5.5 |
| 交换时机 | 周期性交换，不并入配对（配对只发生一次）。实现改为**独立的 5 分钟周期**，不并入 `sync_exchange` 的 30s 轮次 | §5.5 / §7.1 |
| 传播边界 | 只在 `full ↔ full` 交换，且只引荐 `full`；不引荐 `friend`（社交图泄露） | §5.5 / §5.9 |
| 数据模型 | `devices` 加 `introduced_by` + `introduce_dismissed` 两列，**不新建表、不重建表** | §5.6 |
| 移除语义 | 本机删除立即生效；再次引荐只产生「待确认」，不自动恢复；「忽略」永久静默 | §5.7 |
| 引荐者被移除 | **不级联**移除它引荐过的设备——手机丢了不该让家里和单位的电脑互相不认识 | §5.7 |
| Tailscale/ZeroTier 定位 | 二者都需要账号/控制面，不是本设计的参照物；参照 Syncthing / EasyTier | §2.1 |
| 是否做虚拟网卡 | **不做**，我们只要一条应用层连接 | §2.3 / §6.4 |
| IPv6 的作用边界 | 替掉的是**打洞与中继**，不是汇合点；地址仍需发现 | §3.2 / §6.1 |
| 「零第三方」的真实边界 | 不需要服务商，但需要你自己有个稳定入口（DDNS / 固定地址） | §6.3 |

## 9. 仍待确认（R2 实现时已定，留档）

1. ~~**引荐是否要带「最后在线地址」？**~~ **已定：只带 `server_hint`，不带 `last_addr`**——带上能加速首次连接，但会把内网地址暴露给一台用户还没确认的设备，不划算。
2. ~~**多跳传递的深度**~~ **已定：会多跳，但每一跳都要用户确认一次**，且**待确认记录本身不参与转发**（只转发 `trusted = 1` 的行）。这正是 (C) 不会失控的原因。UI 已按 §5.8 说清「谁说的」。
3. **待确认列表的容量上限**与单次引荐条数上限的具体数值（§5.9 拒绝服务缓解）。
4. **R4 内置 server 模式**是否会与桌面端现有的 sidecar 进程管理（aria2 / transmission / llama-server）在生命周期上冲突，需要在动手前小样验证。
5. IPv6 打分的具体规则（ULA、临时地址 / privacy extensions 的取舍）需要在真实双栈环境实测后定稿，不凭文档拍。
