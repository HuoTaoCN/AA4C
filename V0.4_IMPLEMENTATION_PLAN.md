# AA4C V0.4 Implementation Plan（Download）

> 依据 [DOWNLOAD_DESIGN.md](DOWNLOAD_DESIGN.md)（v3：D2 引擎从 qBittorrent 换成 Transmission + 预留 Lua 插件边界，见其 §3.6/§10；§9 是已确认决策，**不要重开已定案的讨论**）。
> 给执行 Agent：开工前先读 [AGENTS.md](AGENTS.md)（工程约定）、[HANDOFF.md](HANDOFF.md)（环境坑 + 教训，尤其第五节）。
> 每个里程碑独立提交、独立验收；提交前自检 `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`（+ 前端改动时 `pnpm build`）。接口变更**先改文档**（DATABASE_SCHEMA / API_DESIGN）再写代码。
> **V0.4 不碰设备间线路协议**：`PROTO_VERSION` 不动、`aa4c-proto` 不动——下载是纯本机能力，没有对端设备。这也意味着 V0.4 各里程碑与旧版本客户端天然互通，无兼容性注意事项。

## V0.4 目标与验收（Definition of Done）

在桌面三平台（Windows / macOS / Linux）上：

1. 粘贴一条 HTTP/HTTPS/FTP 直链即可下载：实时进度/速度/ETA，可暂停/继续/取消，完成后能打开所在文件夹；
2. 应用退出重启后，未完成的下载自动恢复（字节级续传），历史记录不丢；
3. （D2）粘贴 magnet 链接可下载 BT 内容；
4. 全程用户不需要安装、配置任何外部工具；aria2c/transmission-daemon 对用户不可见（无终端窗口、无孤儿进程、UI 无技术术语）；
5. 下载内容默认**不**进入任何同步范围——不会未经确认被分享/同步给已配对设备。

## 里程碑总览

| 里程碑 | 内容 | 前置 | 交付判定 |
|--------|------|------|----------|
| D1 | Aria2 集成（HTTP/HTTPS/FTP）+ 引擎二进制流水线 | — | e2e：加任务→完成→内容哈希对；暂停/继续/取消；重启恢复；三平台安装包内置 aria2c 可用 |
| D2 | Transmission 集成（BT/Magnet，v3 换引擎） | D1 | e2e：magnet → 落盘（用本地做种的测试 torrent）；BT 任务与 D1 任务并列显示 |
| D3 | 统一任务中心打磨 | D1（D2 可并行） | 设置页「下载」区块（目录/限速/并发）；批量操作；下载目录同步范围警示 |

里程碑完成后照例更新 CHANGELOG / HANDOFF（进度表 + 下一步）/ DOWNLOAD_DESIGN 的「已实现」标注。

---

## D1 — Aria2 集成（细化到步骤）

**目标**：`aa4c-download` crate + aria2c sidecar 全生命周期 + `download_tasks` 表 + Core/Tauri/UI 接线 + 引擎二进制自建流水线。完成后 V0.4 DoD 的 1/2/4/5 成立。

**实现顺序刻意把"引擎二进制流水线"放在代码之后**：开发与测试全程用 PATH 里的系统 aria2c（`brew install aria2` / `apt install aria2` / `choco install aria2`，CI 同样安装），`SidecarSpawner` 的依赖倒置保证了这一点是零成本的——`ProcessSpawner`（tokio::process，从显式路径或 PATH 拉起）本来就是 Docker/headless 场景要的实现，测试直接用它，不碰 Tauri。打包流水线是纯 CI 工作，放最后做完即验收。

### 前置阅读

- DOWNLOAD_DESIGN §2（SidecarSpawner 边界）、§3（conf 生成 / WS 单连接 / GID 映射 / **§3.4 重启对账**）、§9（决策表，尤其 v2 新增行）
- DATABASE_SCHEMA §4e（`download_tasks` 定稿）
- `crates/aa4c-core/src/server_link.rs`：`SignalChannel` 的 pending oneshot 表——`Aria2Client` 的 JSON-RPC id 关联直接照这个模式写
- `crates/aa4c-transfer/src/lib.rs`：`TransferService::new` 返回 `Arc<Self>` + `set_*` 注入钩子的装配模式；事件如何进 Core 总线
- `crates/aa4c-core/tests/core.rs` 的 `spawn_node` 手法；C1 的 UDP 黑洞代理（手写测试基础设施的先例——D1 的本地 HTTP 文件服务器同理手写，不引 hyper）

### 步骤

1. **文档先行**：DATABASE_SCHEMA §4e 标注迁移号 `008_downloads.sql`（user_version=8）；API_DESIGN 命令表追加 `add_download` / `pause_download` / `resume_download` / `cancel_download` / `list_downloads` 五行（参数/返回值形状照 §4e 与 CoreEvent 定稿写清）。
2. **`aa4c-types`**：`DownloadTask` 结构体（字段与 §4e 一一对应，serde camelCase，同 `TransferTask` 惯例）+ `DownloadStatus` 枚举（`active`/`waiting`/`paused`/`error`/`complete`/`removed`，小写序列化）；`CoreEvent` **只追加**三个变体：`DownloadProgress{task_id, downloaded_bytes, total_bytes, speed_bps}` / `DownloadDone{task_id, save_path}` / `DownloadFailed{task_id, error}`；JSON 形状测试（对照既有 `TransferConnected` 形状测试）。
3. **`aa4c-store`**：迁移 `008_downloads.sql`（§4e 原文照录）；CRUD：`insert_download` / `get_download` / `list_downloads`（按 `created_at` 倒序）/ `update_download_status` / `update_download_progress`（节流由调用方负责，Store 不管频率）/ `list_unfinished_downloads`（对账用：status IN active/waiting/paused）；测试：迁移版本断言升 8 + `download_crud_roundtrip`。
4. **新 crate `crates/aa4c-download`**（workspace 成员，依赖 aa4c-types/aa4c-store/tokio/tokio-tungstenite/serde_json/uuid/bs58/tracing，**不依赖 tauri**）：
   - `spawner.rs`：`SidecarSpawner` trait（`spawn(conf_path) -> Result<Box<dyn EngineChild>>`；`EngineChild`: `kill()` + `wait()`）+ `ProcessSpawner` 公开实现（tokio::process::Command，显式二进制路径或 PATH 查找；这是测试与将来 Docker/headless 共用的实现，不是测试专用桩）。
   - `conf.rs`：端口探测（`TcpListener` bind 127.0.0.1:0 取端口后释放）+ 密钥生成（2×UUIDv4 → bs58，同 C6 `generate_token` 先例，不引新 RNG）+ conf 文件生成（§3.1 定稿的字段；Unix `PermissionsExt` 设 0600；`input-file` 行仅当 session 文件已存在时写入）。
   - `rpc.rs`：`Aria2Client`——单条 WS 连接（`ws://127.0.0.1:<port>/jsonrpc`），`call(method, params)` 自动注入 `token:<secret>` 首参，JSON-RPC id → oneshot pending 表关联响应（照 `SignalChannel`）；通知（`aria2.onDownload*`）从同一读循环分流出去；断线重连（递增退避）+ 重连成功后通知上层跑一次对账。
   - `lib.rs`：`DownloadService::start(spawner, store, data_dir, download_dir, events) -> Arc<Self>`——生成 conf → spawn → 健康检查（`aria2.getVersion` 有限次递增重试；**端口被抢竞态**：aria2c 退出/健康检查失败时换端口重拉，与重试共用同一套退避，见 §3.1）→ **启动对账**（§3.4 定稿逻辑：`tellActive`/`tellWaiting`/`tellStopped` 全量 vs `download_tasks`，孤儿未完记录标 error，aria2 有表里没有的补插）；`add(url) -> gid`（`addUri` → `insert_download`）；`pause`/`resume`/`cancel`（`pause`/`unpause`/`remove`）；事件循环：WS 通知 → `tellStatus(gid)` 补详情 → 广播 `CoreEvent` + 写库（**状态迁移必写，进行中数秒级节流**，§4）；低频 `tellActive` 轮询兜底（数秒级，防 WS 静默漏事件）；`shutdown()`：`aria2.shutdown` → 超时强杀（`EngineChild::kill`）。启动失败（探测不到 aria2c/健康检查耗尽）= 下载能力整体降级不可用，**不阻塞调用方**，同既有可选能力先例。
   - **crate 级集成测试**（真 aria2c，PATH 安装；找不到二进制时**显式 panic 报"需要安装 aria2"**，不静默跳过——静默跳过会烂）：手写一个回环 HTTP/1.1 静态文件服务器（tokio TcpStream，几十行，同 C1 黑洞代理先例）；测试：① 完整下载 → 内容 BLAKE3 对上 + 状态机走完 `active→complete`；② 暂停→继续→完成；③ 取消后 aria2 与表状态一致（removed）；④ 404 → `error` + 人类可读 error 字段；⑤ **重启恢复**：下载中途 `shutdown()` 整个 service → 用同一 data_dir/Store 重新 `start()` → 断言经 session 装回后同 GID 继续到完成（DoD 第 2 条的直接验证）；⑥ 表里有未完记录但删掉 session 文件再启动 → 对账标 error。
5. **Core 接线**：`CoreConfig` 加 `download_spawner: Option<Arc<dyn SidecarSpawner>>`（`None` = 无下载能力，Android/未注入场景自然降级）；`Settings` 加 `download_dir`（`KEY_DOWNLOAD_DIR`，默认 `dirs::download_dir()`，**不套 `AA4C` 子目录**——见 §5：必须在 save_dir=`~/Downloads/AA4C` 子树之外，`~/Downloads` 本身满足）；`Core::start` 第 9 步后装配 `DownloadService`（spawner 为 Some 时）；`orchestrate.rs` 加 `add_download`/`pause_download`/`resume_download`/`cancel_download`/`list_downloads`（download 服务不可用时返回带 `code()` 的明确错误，前端可转译）；`Core::shutdown` 顺序：download 先于 store 关。
6. **Tauri 壳层**：`apps/desktop/src-tauri` 加 `tauri-plugin-shell` 依赖并注册；`SidecarSpawner` 适配实现（`ShellExt::shell().sidecar("aria2c")`，注意 tauri sidecar 名不带 target-triple 后缀，Tauri 自己解析）；capability 里 shell 权限用参数校验器精确放行 `--conf-path=<data_dir 前缀>`（§3.1 收敛命令行形状的收益就在这里兑现）；5 个 Command + `event_payload` 三个新分支。**桌面注入 spawner，Android 构建不注入**（cfg 或运行时判断，下载页在 Android 显示"桌面版功能"占位——V0.4 范围决定）。
7. **前端**：`types.ts`/`api.ts` 加 `DownloadTask` + 5 个绑定；`stores/download.ts`（load/add/pause/resume/cancel + 事件驱动的进度更新，节流模式照 `useTransferStore`）；`DownloadPage.vue` 替换 `UnderConstruction` 占位（§6 定稿：顶部链接输入 + 任务列表复用 TransferCard 视觉语言 + 每条任务的暂停/继续/取消/打开所在文件夹）；错误/状态转译进 `format.ts`（不出现 aria2/GID/RPC 字样）。Vite 预览走查（同 C6 SharePage 先例）。
8. **引擎二进制流水线**（代码全绿后做）：
   - `.github/workflows/engines.yml`（**只 workflow_dispatch 手动触发**，输入 aria2 版本号）：macOS job 从官方源码 tag 编译 x86_64 + aarch64（AppleTLS，静态）、Linux job 编译 x86_64（musl + 静态 OpenSSL）、Windows job 下载官方 release zip 取出 aria2c.exe；统一算 SHA-256，产物 + `SHA256SUMS` 上传到 release tag `engines/aria2-<版本>`。
   - `scripts/fetch-engines.sh`：按仓库里写死的校验和从 engines release 下载/校验/改名放进 `src-tauri/binaries/`（校验和写在脚本头部常量区，升级引擎 = 改这里 + 走一次 engines workflow，diff 可审）；提供 `--from-path` 开发兜底（复制 PATH 里的 aria2c 并按当前平台 triple 改名，**仅本地开发**，脚本里注明）。
   - `tauri.conf.json` 加 `bundle.externalBin: ["binaries/aria2c"]`；`.gitignore` 排除 `src-tauri/binaries/`（校验和入库，二进制不入库）。
   - `release.yml` 三平台 build job 在 tauri-action 之前插一步 `bash scripts/fetch-engines.sh`（Windows runner 跑 bash 没问题）。
   - `ci.yml` Test job 三平台各加一步安装 aria2（apt/brew/choco）——crate 集成测试需要；HANDOFF 环境安装节同步加一行 `brew install aria2`。
9. **人工走查**：`pnpm tauri dev`（先 `scripts/fetch-engines.sh --from-path`）——粘贴一个真实 URL 下载、暂停/继续、退出重启验证恢复、完成后打开文件夹；确认无终端窗口闪现（Windows 重点看）、退出后无 aria2c 残留进程（`ps`/任务管理器）。
10. **文档收尾**：DOWNLOAD_DESIGN §3/§4/§5 标「已实现」+ 实现偏差记录；DATABASE_SCHEMA §4e 标已建表；CHANGELOG Unreleased；HANDOFF 进度表 + 下一步指向 D2。

### 验收清单

- [ ] `cargo test --workspace` 全绿（含 aa4c-download 集成测试 6 条；CI 三平台已装 aria2）
- [ ] fmt / clippy -D warnings 干净；`pnpm build` 通过
- [ ] 人工走查（步骤 9）全过，退出无孤儿进程
- [ ] engines workflow 三平台产物齐 + 校验和入库；release.yml 干跑（可临时 tag 或 workflow_dispatch 验证 fetch 步骤）
- [ ] 既有功能零回归（V0.1–V0.3 全部旧测试不动照过；`PROTO_VERSION` 未动）

### 明确不做（D1 范围外）

Transmission/BT（D2）；设置页下载区块与限速/并发透传（D3）；批量操作（D3）；下载失败自动重试、子进程自动重启（观察后再定）；Android 下载（V0.4 范围外）；S3（范围外）。

---

## D2 — Transmission 集成（BT/Magnet）

> v3 换引擎：原计划的 qBittorrent 经三平台 headless 分发逐项核实后放弃（Windows/macOS 均无可用先例，Windows 完全空白），换成 Transmission——官方 Windows MSI 自带 `transmission_daemon.exe`（已拆包核实）、macOS/Linux 有 Homebrew core 既验证的 CMake 构建配置可直接进 engines.yml。设计细节全部在 DOWNLOAD_DESIGN.md §3.6，此处只列步骤级要点。

- **前置小样验证（编码前必做）**：孤儿进程防护——Transmission 没有 `stop-with-process` 等价物，候选组合（Windows Job Object / Linux `PR_SET_PDEATHSIG` / macOS PID 文件 + 启动清扫）先各自小样验证（尤其确认 Tauri sidecar 机制下能否拿到原始进程句柄做 Job Object 归属），定稿写回 DOWNLOAD_DESIGN §9。
- transmission-daemon sidecar：**必须 `-f` 前台模式**（默认 fork 后台化会让进程句柄抓错对象）；`--config-dir <data_dir>/transmission`，`settings.json` 每次启动整体重写（127.0.0.1 绑定 + 随机端口 + 随机 Basic 凭据，凭据不进命令行，同 D1 先例）。
- `TransmissionClient` 独立实现（HTTP POST 单端点 + 409 握手取 `X-Transmission-Session-Id`，手写极简 HTTP/1.1 客户端，不引 reqwest）；**无事件推送**——直接以 D1 已收敛的 `reconcile()` 轮询为主路径（`torrent-get` 全量对账），不需要新机制。
- `add_download` 改走引擎无关请求描述中间层（URL + 请求头 + 引擎选项 + 保存子路径），按 scheme 分流（`magnet:` → Transmission，其余 → aria2）——同时是 DOWNLOAD_DESIGN §10 插件系统 `on_before_add` 的预留接缝。
- `download_tasks.kind='bt'` 分支：id 用 infohash（引擎原生 id 的同一原则）；做种数/peer 数/分享率只进事件不落库；`percentDone==1` 标 `complete` 但**不停止做种**（§3.6.4 定稿）。
- engines workflow 加 transmission 构建腿：Windows 官方 MSI `msiexec /a` 静默解包提取（**注意 DLL 伴随文件清单**，sidecar 之外要走 `bundle.resources`，aria2 单文件静态没有这个问题）；macOS/Linux 用 Homebrew formula 的 CMake 配置自建——按 aria2 首跑的四个坑预设排查路径（HANDOFF 第五节）。
- e2e：本地做种一个测试 torrent（transmission 自带工具或测试内起轻量做种端）→ magnet 添加 → 落盘校验。CI 里 BT 回环可行性先验证，不行就降级为"本地人工走查 + API 层单测"（同 C5 打洞真实 NAT 的处理先例——CI 测接线，真实场景人工验证）。

## D3 — 统一任务中心打磨

- 设置页「下载」区块：下载目录（含**同步范围重叠警示**，§5 定稿：警示不硬禁）、全局限速、并发数、BT 分享率/做种时长限制——透传 aria2 `max-overall-download-limit`/`max-concurrent-downloads` 与 Transmission `session-set` 对应项（写进 conf/启动配置，改动后重启引擎生效即可，不追求热更新）。
- D1+D2 任务同列表按 `created_at` 统一排序；批量操作：全部暂停/全部继续/清除已完成记录。
- 收尾：错误转译补全（aria2 错误码 → 人话对照表）、下载完成系统通知（复用既有通知惯例）。

## 风险与注意事项

- **aria2 静态编译是 D1 最大的不确定项**（autotools + TLS 后端组合），但它被刻意排到代码之后且与代码零耦合——真被卡住时，短期兜底是官方 Windows 产物 + macOS/Linux 暂用 `--from-path`（开发不受阻），流水线单独攻坚，不阻塞 D1 其余验收项。
- **Windows 上子进程窗口闪现**：tauri-plugin-shell 应已处理 CREATE_NO_WINDOW，但要在走查里实测确认（步骤 9 专门列了）。
- **测试用真 aria2c 意味着测试有外部依赖**：CI 三平台安装步骤是硬前提，本机开发也要装（HANDOFF 环境节同步）；找不到二进制的测试显式 panic 报安装指引，绝不静默跳过。
- **`cargo test --workspace` 跨 crate 并行**（HANDOFF 教训）：aa4c-download 的集成测试各自随机端口 + 独立 data_dir，天然并行安全，但注意别复用固定端口写死。
- **conf/session 文件路径含空格/中文**（用户目录常见）：conf 生成与 fetch 脚本都要引号意识；走查用带空格路径试一次。
- bincode/线路协议**完全不涉及**——不存在追加纪律问题，但同样意味着别顺手改 `aa4c-proto`（没有理由）。

## 范围外（明确不做，防止蔓延）

自研下载/BT 引擎；嵌入/链接 GPL 代码；`--rpc-listen-all` 对外暴露 RPC；BYO 外部 daemon 模式（连接用户自己起的 aria2/Transmission）；下载内容病毒扫描；浏览器插件/浏览器接管下载；RSS 订阅下载（将来归 Lua 插件里程碑，DOWNLOAD_DESIGN §10）；Lua 插件系统本体（V0.4 只留接缝，实现是独立里程碑）；Android 下载能力；S3。
