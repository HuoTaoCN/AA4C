# AA4C UI Design Spec

> AA 交互设计规范。技术栈：Tauri 2 + Vue3 + TypeScript + Pinia，**桌面端与 Android 共享同一前端**。
> 与 [API_DESIGN.md](API_DESIGN.md) §9 的 Tauri Commands / Events 一一对应。
> §2–§9 以桌面布局为基准描述；Android 的差异集中在 §11。
>
> **本文档覆盖全部页面。** V0.8「Focus」F3 之前它只写到里程碑 C6（分享页），
> 而当时最大的两个页面——归档页 825 行、下载页 552 行——**完全没有规格，是徒手画出来的**。
> 那是「界面杂乱」最直接的成因：没有规格，每个页面就自己发明一套布局、一套卡片、一套
> 间距，21 个组件里堆出约 2100 行私有 CSS。
>
> **纪律：先改这份文档，再改代码。** 新增或重做任何页面，先在 §3 写下它的规格。

## 1. 设计原则

1. **AA 是核心动作**：界面的一切围绕"选文件 → 选设备 → AA"三步完成
2. **面向普通用户**：父母辈用户不看说明书也能完成第一次传输
3. **零专业术语**：UI 永远不出现技术词汇（见 §7 术语表）
4. **状态永远可见**：设备在不在线、文件传到哪了，一眼可见
5. **默认安全，无感安全**：加密、校验、认证全部默认开启，UI 不提供关闭选项
6. **连接是主角**（V0.8 F3 新增）：AA4C 唯一不可替代的能力是「我的几台设备在任何网络下
   自己连成一片，不用注册账号」。界面的第一屏就该回答「它们现在连上了吗、怎么连的」，
   而不是列一堆功能入口。**做到了却看不见，等于没做。**

## 2. 信息架构

```
┌──────────────────────────────────────────────────┐
│ AA连接        我的 MacBook  ● 在线          ⚙     │  ← 顶栏
├──────────┬───────────────────────────────────────┤
│          │                                       │
│  设备    │                                       │
│  传输    │              主内容区                  │
│  同步    │                                       │
│  分享    │                                       │
│  ─────   │                                       │
│  下载    │  ← 「更多」：装了插件才出现            │
│  归档    │                                       │
│          │                                       │
│  设置    │                                       │
├──────────┴───────────────────────────────────────┤
│ ⬆ 正在发送 IMG_2024.jpg 至 客厅电脑  68%  ✕       │  ← 全局任务条
└──────────────────────────────────────────────────┘
```

**主导航四项 + 设置**：设备 / 传输 / 同步 / 分享 / 设置。

### 为什么从 8 项砍到 5 项

F3 之前是 8 个导航目标（首页 / 传输 / 同步 / 分享 / 下载 / 归档 / 记录 / 设置），而且
**首页本身是一个「能力」宫格，格子里链的就是侧栏那 5 项**——菜单的菜单。那是「产品对
用户先做什么没有意见」的典型症状。

合并规则：

| 原页面 | 去处 | 理由 |
|--------|------|------|
| 首页 | **变成「设备」** | 第一屏回答「我的设备连上了吗」，而不是列功能入口 |
| 记录 | 并入**传输** | 记录就是传输的历史，本来就是同一件事的两个时态 |
| 分享 | **保留独立入口** | 它是 V0.3 的一个完整能力（链接 + 有效期 + 吊销 + 访问记录），塞进传输页会被埋起来。与传输的区别也清楚：传输给**已配对的设备**，分享给**还没配对的人** |
| 下载 / 归档 | 进**「更多」分区** | 它们是插件（V0.8 F1）。按 PROJECT_VISION 自己的定义，拿掉它们 AA4C 还是 AA4C |

> 「分享」曾考虑并入「传输」（都在回答「怎么把这个文件给别人」），最终保留独立入口——
> 它有自己的一套对象（链接而非设备）、自己的生命周期（有效期 / 吊销 / 访问记录），
> 折进传输页只会变成一个被忽略的折叠区块。

**「更多」分区只在装了对应插件时出现**——这正是本文档 F3 之前就写过、却被违背了的原则：
「Sync / Share / Download / AI 的导航项 V0.2+ 再出现，**不提前占位**」。空的插件注册表
应当得到一个只有三项导航的干净界面。

---

## 3. 页面规格

### 3.1 设备（首页）

**这一屏要回答的唯一问题：我的设备现在连上了吗，怎么连的，连不上该怎么办。**

| 区块 | 内容 | 数据来源 |
|------|------|----------|
| 本机 | 设备名、平台、监听状态；对外可达性提示（见下） | `get_self_device` / `local_server_status` |
| 待确认的设备 | 引荐来的设备，**有才显示**（TRUST_DESIGN §5.8） | `list_pending_introductions` |
| 我的设备 | 已配对设备，每台一行，带连接档位 | `list_devices` + `list_reachability` |
| 附近可配对 | 局域网发现、尚未配对的设备 | `list_devices` + `device_found/lost` |

#### 设备行（`DeviceRow`，见 §5.3）

```
💻  家里的台式机                            已连接 · 局域网直连
    我的设备 · 3 分钟前                                  [发送]
```

连不上时**必须给下一步**，不能只说坏了：

```
🖥  单位的台式机                                        连不上
    我的设备 · 上次 2 小时前经中转站连上
    不在同一个网络，而且远程连接没开。
    把两台设备连到同一个 WiFi，或在设置里打开远程连接。      [去设置]
```

还没探测过时：

```
📱  我的手机                                        正在检查…
```

> **「正在检查」不是凑数的状态。** 刚启动时确实还没探测过，这时说「离线」是在编造一个
> 我们并不知道的事实。后端为此专门区分了 `unknown` 与 `unreachable`
> （`aa4c_types::ReachState`）。

#### 连接档位的文案（唯一出处：`lib/format.ts`）

| 档位 | 文案 | 说明 |
|------|------|------|
| `lan` | 局域网直连 | 同一个网络，最快 |
| `public_v4` | 公网直连 | 跨网络直连 |
| `public_v6` | 公网直连（IPv6） | 同上；IPv6 单独标出来，排查时最有用 |
| `punch` | 直连 | 打洞只是「怎么找到对方」，连上就是真直连；**不出现「打洞」二字**（§7） |
| `relay` | 中继（较慢） | 唯一需要提醒的一档：慢，而且要有中转站 |

#### 行内操作

- 「发送」——跳到传输页并预选这台设备
- 「这是我的设备」开关——升 / 降完全信任（`set_trust_level`）。降级时提示一句
  「它的文件会从同步视图里消失，本机已下载的文件不动」，**不弹确认框**（可逆）
- 待确认的设备：「标记为我的设备」/「忽略」（`confirm_introduction` / `dismiss_introduction`）
- **解除配对不在这里**——不可逆操作放设置页，理由见 §3.5

空状态（一台设备都没配对）：

> 还没有连接任何设备。
> 在另一台设备上打开 AA连接，连到同一个 WiFi，它就会出现在下面。

### 3.2 传输

两个区块竖排，**不是两个标签页**——「发」和「发过什么」是同一件事的两个时态，
切标签会让人为了看一眼刚才发成功没有而离开当前页。

**① 发送**（原 AA 页，三步流不变）

```
第 1 步 选文件        第 2 步 选设备         第 3 步 AA
┌───────────────┐   ┌───────────────┐   ┌───────────┐
│  拖文件到这里  │ → │ ○ 客厅电脑 ●   │ → │  [ AA！]  │
│  或点击选择    │   │ ○ 妈妈的手机 ● │   │           │
└───────────────┘   └───────────────┘   └───────────┘
```

- **拖拽**：整个窗口都是拖放目标；拖入时全窗口高亮，显示「松开，把文件 AA 出去」
- **多选**：文件 + 文件夹混合；列表显示文件名、大小、总计
- **设备选择**：只列已配对且在线的；离线的置灰并显示可达性原因（数据同 §3.1）
- 调用：`send_files(deviceId, paths)`

**② 记录**（原记录页）

- 倒序，分组：今天 / 昨天 / 更早
- 每条：方向（⬆/⬇）、对端、文件名（多文件显示「xx 等 N 个文件」）、大小、状态、时间
- 成功的接收 →「打开所在文件夹」；失败的 → 原因 +「重试」

### 3.3 同步

跨自己设备的统一文件视图（设计见 [SYNC_DESIGN.md](SYNC_DESIGN.md)），内容不变：

- 文件/目录树并集，每项一个状态点：🟢 本地有 / 🟡 可下载 / 🔴 设备离线
- 点 🟡 →「正在从 <设备名> 取回…」→ 转 🟢；点 🔴 → 「等 <设备名> 上线后可取」
- 同名不同内容并列展示、加序号（`报告.pdf` / `报告 (2).pdf`），不自动覆盖
- 「收到的」按「来自 <设备名> · 时间」分组
- 顶部筛选：全部 / 仅可下载 / 仅本地
- 空状态：「还没有把任何设备设为『我的设备』。在设备页把你自己的另一台设备标记一下，
  就能在这里看到它的文件。」

### 3.4 分享

给**还没配对的人**一条链接（设计见 [CONNECT_DESIGN.md](CONNECT_DESIGN.md) §7）。
与「传输」的分界：传输的对象是设备，分享的对象是一条链接。

- **生成**：从本机已有（🟢）的文件里选一个 + 有效期（1 小时 / 1 天 / 7 天 / 长期）→
  「生成链接」→ 自动复制到剪贴板 + toast，**不弹窗展示链接**（列表里随时可再复制）。
- **打开别人的**：粘贴 `aa4c://share/…` → 「打开」→ 进度走同一套传输卡片与任务条，
  不另做一套「分享下载」UI。
- **我的分享**：目标路径、状态（长期有效 / N 天后过期 / 已过期 / 已吊销）、链接（等宽字体，
  一键复制）、「吊销」（已吊销置灰）。
- 空状态：「还没有生成过分享链接。」；没有本地文件可选时引导去「同步」页添加同步文件夹。
- 打开失败**统一报「链接无效或已过期」**，不区分具体原因——不泄露某条分享是否存在过
  （同后端的防探测惯例）。

> **仍未做**（V0.8 F5）：`aa4c://` 的系统级 deep-link 注册与二维码。「把文件 AA 给我」
> 这句传播语目前落不了地——用户只能粘贴一串 base58。

### 3.5 设置

**目标：2 个分区、10 项，页面 300 行以内**（F3 之前是 5 个标签页、28 项、903 行——
其中 18 项属于下载与归档，那是设置页失控的直接原因）。

**① 这台设备**

| 设置项 | 控件 | key |
|--------|------|-----|
| 设备名称 | 文本框 | `device_name` |
| 接收文件保存到 | 目录选择器 | `save_dir` |
| 我的设备来的文件自动接收 | 开关（默认关） | `auto_accept_from_trusted` |

**② 让不同网络的设备也能连上**

| 设置项 | 控件 | key |
|--------|------|-----|
| 中转站地址 | 文本框 | `server_url` |
| 开启远程连接 | 开关（默认关，地址为空时禁用） | `enable_remote` |
| 自动在路由器上开端口 | 开关（默认开，被上一项罩着） | `enable_port_mapping` |
| 让这台设备当中转站 | 开关（默认关）+ 对外地址 + 端口 | `enable_local_server` 等 |

**③ 插件设置**——**不手写**。每个插件用 `settings_schema()` 声明自己的字段，
由一个通用渲染器画表单（`PluginSettings.vue`）。这是设置页此前为下载写 12 项、
为归档写 6 项、涨到 903 行的根治办法。

#### 已配对设备的管理拆在两处

按**可逆性**分，不按「都放一起更整齐」分：

| 操作 | 位置 | 理由 |
|------|------|------|
| 「这是我的设备」升 / 降级 | **设备页**（§3.1，行内开关） | 可逆、会反复调整，就该在看得见设备的地方顺手完成 |
| 确认 / 忽略引荐 | **设备页**（§3.1「待确认的设备」） | 同上；而且它天然属于「我的设备网络」这一屏 |
| **解除配对** | **设置页 → 已配对设备** | **不可逆**。放在设置里更「重」，要多走一步才点得到——这正是想要的摩擦 |

设置页那份列表同时是「我到底配过哪些设备」的完整清单（含当前不在线、设备页可能没在
显眼处列出的），所以它保留全部设备与信任级别的只读展示，只把危险操作放在这里。

### 3.6 「更多」：插件页面

装了插件才出现。页面本身的交互设计在各自的设计文档里
（[DOWNLOAD_DESIGN.md](DOWNLOAD_DESIGN.md) / [ARCHIVE_DESIGN.md](ARCHIVE_DESIGN.md)），
但**必须使用 §5 的共享组件**，不得再自己发明卡片与间距——F3 之前它们各写了
191 行与 230 行私有 CSS，那正是要收掉的东西。


## 4. 核心流程

### 4.1 首次启动

1. 欢迎页一句话："连接你的所有设备" + "开始"按钮
2. 输入设备名（预填 hostname，如"Huo 的 MacBook"）
3. 进入首页，开始发现设备

不需要注册、登录、账号。

### 4.2 设备配对（双向 PIN 确认）

```
发起方 A                              接收方 B
   │  点设备卡片上的"配对"                 │
   │ ───────── 配对请求 ──────────▶  弹窗："Huo 的 MacBook
   │                                想与本机配对" [接受][拒绝]
   │                                       │ 接受
   │ ◀──────── 双方各自显示 PIN ──────────  │
   │                                       │
┌──┴────────────┐                 ┌────────┴──────┐
│ 确认码 4 7 2 9 1 5 │                 │ 确认码 4 7 2 9 1 5 │
│ 两台设备数字一致？  │                 │ 两台设备数字一致？  │
│  [一致，配对]      │                 │  [一致，配对]      │
└───────────────┘                 └───────────────┘
   │  双方都确认 → "配对成功 🎉"              │
```

- 事件链：`start_pairing` → `pairing_request`（B 端）→ `confirm_pairing` → `pairing_pin`（双端）→ `confirm_pairing` → `pairing_result`
- PIN 显示为大号数字，两端字体、分组（3+3）完全一致，方便目视比对
- 任一方拒绝或 60 秒超时 → "配对未完成，可以重新发起"

### 4.3 接收文件

对方发来文件时弹窗（除非开启自动接收）：

> **客厅电脑** 想把 **3 个文件（1.2 GB）** AA 给你
> 保存到：~/Downloads/AA4C（可更改）
> [接收] [拒绝]

调用 `accept_transfer(taskId, accept, saveDir)`。

### 4.4 传输完成

- 成功：系统通知 + 应用内 toast："已收到来自客厅电脑的 3 个文件"，点击打开文件夹
- 失败：toast 显示人话原因（见 §6），记录页可重试

## 5. 设计系统：token 与共享组件

### 5.0 为什么需要这一节

F3 之前，`styles.css` 只有 124 行、20 个 token，然后 **21 个组件各自写了共约 2100 行
私有 CSS**——`.card` `.field` `.hint` `.muted` `.btn` 每个页面重新发明一遍，间距靠手感，
字号各页不同。设计规范里承诺的「lucide 线性图标，V0.2 替换 emoji」到 V0.7 还是 9 个 emoji，
图标库连依赖都没加。

**这条指标当初写错了。** F3 落地时按「私有 CSS 总行数 < 500」去量，结果发现总行数
里有一大半是**真正的页面专属布局**——发送页的三步网格、AA 圆钮、拖放区，下载页的
高级选项折叠区，归档页的问答框与规则表单。那些本来就该留在各自页面，压掉它们只会
把布局塞进共享组件、换来更糟的耦合。

**真正要消灭的是重复，不是行数。** 改用两条可以 grep 的判据：

1. **没有任何页面再自己发明 `.card` / `.btn` / `.empty` / `.list` / `.field`**
   ——它们在共享组件里各只有一份。
2. **间距、字号、字重不出现字面量**，一律走 token；组件专属尺寸（圆点直径、
   胶囊圆角、列宽）不在此列，它们不属于任何阶梯。

F3 收尾时两条都满足；总行数从约 2100 降到约 1560，其中共享组件 294 行是新增的
**被复用**的部分。

### 5.1 Token

全部定义在 `apps/desktop/src/styles.css` 的 `:root`，深色模式只重定义颜色。
**组件里不许出现字面量**（`14px` / `#e3e6ea` / `8px`）——一律用 token。

| 组 | token | 值 |
|----|-------|-----|
| 间距 | `--sp-1` … `--sp-6` | 4 / 8 / 12 / 16 / 24 / 32 px |
| 字号 | `--fs-sm` `--fs-base` `--fs-lg` `--fs-xl` | 12 / 14 / 16 / 20 px |
| 字重 | `--fw-normal` `--fw-medium` `--fw-bold` | 400 / 500 / 600 |
| 圆角 | `--radius-sm` `--radius` `--radius-lg` | 9 / 14 / 20 px |
| 层级 | `--z-bar` `--z-sheet` `--z-toast` | 10 / 100 / 1000 |
| 动效 | `--motion-fast` `--motion-base` | 120ms / 200ms（`prefers-reduced-motion` 时归零） |
| 颜色 | 沿用既有 `--aa-*`，补 `--aa-warn` | 品牌 / 背景 / 表面 / 文字 / 边框 / 成功 / 警告 / 危险 |

### 5.2 共享组件（`components/ui/`）

八个，够用且不多。**新页面不得绕开它们自己写一套。**

| 组件 | 用途 | 关键 props |
|------|------|-----------|
| `Card` | 圆角表面容器，全应用唯一的卡片实现 | `padding`、`interactive` |
| `ListRow` | 一行：图标 + 主副文案 + 右侧状态 + 操作区 | `icon`、`title`、`subtitle`、`tone` |
| `Field` | 设置项一行：标签 + 控件 + 提示 | `label`、`hint`、`disabled` |
| `Toolbar` | 页面顶部标题行 + 右侧操作 | `title`、`actions` 插槽 |
| `EmptyState` | 空状态：一句话说明 + 一句话下一步 | `title`、`hint`、`action` 插槽 |
| `StatusDot` | 状态圆点 + 文案 | `tone: ok \| warn \| danger \| muted` |
| `Sheet` | 底部/居中弹层，替代各页自己写的 dialog | `open`、`title` |
| `Button` | 唯一的按钮实现 | `variant: primary \| ghost \| danger`、`size` |

### 5.3 页面专属组件

| 组件 | 说明 |
|------|------|
| `DeviceRow` | 设备页一行（§3.1）：平台图标、设备名、信任级别、**连接档位**、连不上时的下一步 |
| `TransferCard` | 传输卡片：方向、对端、进度条、速度、ETA、连接档位徽标 |
| `TaskBar` | 全局任务条：吸底，显示最活跃的一条 + 总数，点击展开 |
| `PluginSettings` | 按插件的 `settings_schema()` 渲染表单（§3.4 ③） |

`TransferCard` 的既有规格不变：

```
⬆ 发送至 客厅电脑                    取消 ✕
IMG_2024.jpg（3/5 个文件）
████████████░░░░░░░░  68% · 42 MB/s · 剩约 12 秒
```

- 进度条更新 ≤ 10 次/秒（事件已节流）
- 速度自动换单位；剩余时间用「剩约 X 秒/分钟」
- 连接档位徽标**只在这次连接是自己发起时显示**（发送/拉取）；被动接收的一方不显示——
  不确定就不显示，比猜一个诚实

### 5.4 图标

**引入 lucide，替换全部 emoji。** 这条在本文档 §8 里从 V0.2 就写着，欠了五个版本。

- 导航、操作、状态一律用 lucide 线性图标
- **平台图标是例外**：💻🖥📱🗄 这几个 emoji 辨识度高于任何线性图标，保留


## 6. 状态与文案规范

错误文案必须说**人话 + 给出下一步**：

| 内部错误（code） | UI 文案 |
|------------------|---------|
| `not_paired` | 还没有和这台设备配对，先配对一下吧 |
| `network` / 连接失败 | 连不上对方设备，请确认两台设备在同一个 WiFi |
| `transfer_rejected` | 对方拒绝了这次传输 |
| `hash_mismatch`（重试后仍失败） | 传输出了点问题，文件不完整，请重新发送 |
| `cancelled` | 已取消 |
| 磁盘空间不足（Io） | 空间不够了，清理一下磁盘或换个保存位置 |

设备连不上的原因（`ReachFailure`，V0.8 F2）——**每条都要给下一步**：

| 原因码 | UI 文案 |
|--------|---------|
| `not_on_lan_and_remote_off` | 不在同一个网络，而且远程连接没开。把两台设备连到同一个 WiFi，或在设置里打开远程连接。 |
| `unreachable` | 试过了连不上。确认对方开着 AA连接，并检查防火墙有没有放行。 |
| `peer_refused` | 对方拒绝了连接，多半是版本太旧。确认两台设备都升级到最新版。 |

状态文案：`reachable` →「已连接」，`unreachable` →「连不上」，
**`unknown` →「正在检查…」，不是「离线」**（我们确实还不知道，见 §3.1）。

通用规则：

- 不出现错误码、英文异常、堆栈
- 进行中状态用动词："正在寻找附近设备…"、"正在发送…"、"正在确认文件完整…"

## 7. 术语表（强制）

| ❌ 禁止出现 | ✅ 使用 |
|------------|--------|
| mDNS / 广播 / 发现协议 | 寻找附近设备 |
| 配对 / 密钥交换 / 证书 / 指纹 | 配对、确认码 |
| TLS / 加密握手 | （不提，默认安全；设置页可有一行"所有传输均已加密"） |
| 哈希校验 / BLAKE3 | 确认文件完整 |
| TCP / QUIC / 端口 | （不提） |
| P2P / 节点 | 设备 |
| Task / 任务队列 | 传输、记录 |
| GGUF / 量化 / llama.cpp / RPC | 模型文件（量化规格可在模型库详情里以标签形式展示，如 "Q4"，不展开解释） |
| embedding / 向量 / chunk | 知识库索引 |
| NAT / 打洞 / STUN / 穿透 | （不提；连上了就说「直连」） |
| 中继 / Relay / 服务器 | 中转站 |
| UPnP / 端口映射 | 自动在路由器上开端口 |
| IPv6 | 可以出现——它是用户在路由器/宽带设置里真会看到的词，说「公网直连（IPv6）」比含糊其辞更有用 |
| 插件 / Plugin | 可以出现在「更多」分区与设置里——用户装不装得由他知道 |

## 8. 视觉基调

- **风格**：简洁、留白多、圆角卡片；类 LocalSend / AirDrop 的轻量感
- **品牌色**：`#2F6BFF`（暂定，可后续品牌设计时替换；定义为 CSS 变量 `--aa-primary`）
- **全部数值走 token**，组件里不出现字面量——见 §5.1
- **深色模式**：跟随系统（Tauri `window.theme`），V0.1 必须支持
- **字体**：系统默认字体栈（PingFang SC / Microsoft YaHei / system-ui）
- **图标**：单色线性图标（**lucide，V0.8 F3 起真正引入**——这条从 V0.2 就写着，
  欠了五个版本）；平台图标（💻🖥📱🗄）保留 emoji，理由见 §5.4
- **最小窗口**：900 × 600；所有页面在该尺寸下不出现横向滚动

## 9. 前端状态管理（Pinia stores）

| Store | 状态 | 更新来源 |
|-------|------|----------|
| `useDeviceStore` | 本机信息、设备列表 | `list_devices` + `device_*` 事件 |
| `usePairingStore` | 进行中的配对会话、PIN | `pairing_*` 事件 |
| `useTransferStore` | 进行中任务、历史记录 | `list_transfers` + `transfer_*` 事件 |
| `useSettingsStore` | 设置项 | `get_settings` / `update_settings` |
| `useDeviceStore`（可达性切片） | 每台设备连不连得上、走哪一档 | `list_reachability` + `reachability_updated` 事件 |

事件监听在 App 根组件统一注册（`onMounted` 时 `listen('aa4c://...')`），分发给对应 store，组件只读 store。

## 10. Android 适配

同一前端代码，按视口宽度响应式切换，**不写第二套页面**：

### 布局

- 断点：视口宽度 `< 700px` 进入移动布局
- 侧边导航 → **底部导航栏**，与桌面主导航一一对应（设备 / 传输 / 同步 / 设置，4 个 tab）。
  F3 之前移动端是 5 个 tab（首页/传输/同步/下载/我的），其中「我的」是个把记录、分享、
  归档、设置四样塞在一起的杂物抽屉——那是桌面侧导航过长的连锁后果，现在一并消失了。
  插件页面走顶部「更多」入口，不占底部 tab。
- 设备列表单列；全局任务条吸附在底部导航上方
- 触控目标最小 44×44px；hover 态在触屏上以按压态替代

### 交互差异

| 桌面 | Android |
|------|---------|
| 拖拽文件到窗口 | "选择文件 / 选择照片"按钮（系统文件选择器，`tauri-plugin-dialog`） |
| 点击"打开所在文件夹" | "导出到下载"（V0.2，走 MediaStore）；V0.1 显示保存位置说明 |
| 系统通知（桌面通知中心） | 系统通知（需 `POST_NOTIFICATIONS` 权限，Android 13+ 运行时请求） |
| 最小窗口 900×600 | 全屏；适配刘海区（safe-area-inset） |

### Android 已知限制（界面需如实告知）

- 应用切到后台传输可能中断（前台服务 V0.2 实现）——传输中显示"请保持 AA4C 在前台"
- 接收的文件保存在应用专属目录，卸载应用会一并删除（导出功能 V0.2）

### 文案

§6 / §7 的文案与术语表对 Android 同样生效，无平台例外。
