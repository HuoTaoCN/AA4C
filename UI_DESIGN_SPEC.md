# AA4C UI Design Spec（V0.1 桌面端）

> AA 交互设计规范。技术栈：Tauri 2 + Vue3 + TypeScript + Pinia。
> 与 [API_DESIGN.md](API_DESIGN.md) §9 的 Tauri Commands / Events 一一对应。

## 1. 设计原则

1. **AA 是核心动作**：界面的一切围绕"选文件 → 选设备 → AA"三步完成
2. **面向普通用户**：父母辈用户不看说明书也能完成第一次传输
3. **零专业术语**：UI 永远不出现技术词汇（见 §7 术语表）
4. **状态永远可见**：设备在不在线、文件传到哪了，一眼可见
5. **默认安全，无感安全**：加密、校验、认证全部默认开启，UI 不提供关闭选项

## 2. 信息架构

```
┌─────────────────────────────────────────────┐
│ ◉ AA4C        我的 MacBook ●在线      ⚙ 设置 │   ← 顶栏
├──────────┬──────────────────────────────────┤
│          │                                  │
│  🏠 首页  │                                  │
│  📤 AA   │           主内容区                │
│  📋 记录  │                                  │
│          │                                  │
│          │                                  │
├──────────┴──────────────────────────────────┤
│ ⬆ 正在发送 IMG_2024.jpg 至 客厅电脑  68%  ✕  │   ← 全局任务条（有任务时显示）
└─────────────────────────────────────────────┘
```

V0.1 只有 4 个页面：**首页、AA（发送）、记录、设置**。Sync / Share / Download / AI 的导航项 V0.2+ 再出现，不提前占位。

## 3. 页面规格

### 3.1 首页

| 区块 | 内容 | 数据来源 |
|------|------|----------|
| 本机卡片 | 设备名、平台图标、在线状态 | `get_self_device` |
| 附近设备 | 设备卡片网格（见 §5.1） | `list_devices` + `device_found/lost` 事件 |
| 最近传输 | 最近 5 条记录，可跳转"记录"页 | `list_transfers(5, 0)` |

空状态文案（附近没有设备时）：

> 附近还没有发现设备。
> 在另一台设备上打开 AA4C，并连接同一个 WiFi。

### 3.2 AA（发送页）—— 核心页面

发送三步流，单屏完成：

```
第 1 步 选文件        第 2 步 选设备         第 3 步 AA
┌───────────────┐   ┌───────────────┐   ┌───────────┐
│               │   │ ○ 客厅电脑 ●   │   │           │
│  拖文件到这里  │ → │ ○ 妈妈的手机 ● │ → │  [ AA！]  │
│  或点击选择    │   │ ○ NAS      ○  │   │           │
└───────────────┘   └───────────────┘   └───────────┘
```

交互细节：

- **拖拽**：整个窗口都是拖放目标；拖入时全窗口高亮，显示"松开，把文件 AA 出去"
- **多选**：支持多文件 + 文件夹混合；列表显示文件名、大小、总计
- **设备选择**：在线已配对设备可选；未配对设备显示"先配对"按钮；离线设备置灰
- **AA 按钮**：大、圆、品牌色。点击后立即跳到进度视图（§5.2）
- 调用：`send_files(deviceId, paths)` → 监听 `transfer_progress` / `transfer_done` / `transfer_failed`

### 3.3 记录页

- 倒序时间列表，分组：今天 / 昨天 / 更早
- 每条：方向图标（⬆发送 / ⬇接收）、对端设备名、文件名（多文件显示"xx 等 N 个文件"）、大小、状态、时间
- 操作：接收成功的记录 → "打开所在文件夹"；失败的记录 → 显示原因 + "重试"（重新发起 `send_files`）

### 3.4 设置页

V0.1 只暴露 4 项：

| 设置项 | 控件 | 对应 key |
|--------|------|----------|
| 设备名称 | 文本框 | `device_name` |
| 接收文件保存到 | 目录选择器 | `save_dir` |
| 信任设备来的文件自动接收 | 开关（默认关） | `auto_accept_from_trusted` |
| 已配对设备管理 | 列表 + "解除配对" | `unpair_device` |

## 4. 核心流程

### 4.1 首次启动

1. 欢迎页一句话："让所有设备成为一个空间" + "开始"按钮
2. 输入设备名（预填 hostname，如"Huo 的 MacBook"）
3. 进入首页，开始发现设备

不需要注册、登录、账号。

### 4.2 设备配对（双向 PIN 确认）

```
发起方 A                              接收方 B
   │  点设备卡片上的"配对"                 │
   │ ───────── 配对请求 ──────────▶  弹窗："Huo 的 MacBook
   │                                想与本机配对" [接受][拒绝]
   │                                       │ 接受
   │ ◀──────── 双方各自显示 PIN ──────────  │
   │                                       │
┌──┴────────────┐                 ┌────────┴──────┐
│ 确认码 4 7 2 9 1 5 │                 │ 确认码 4 7 2 9 1 5 │
│ 两台设备数字一致？  │                 │ 两台设备数字一致？  │
│  [一致，配对]      │                 │  [一致，配对]      │
└───────────────┘                 └───────────────┘
   │  双方都确认 → "配对成功 🎉"              │
```

- 事件链：`start_pairing` → `pairing_request`（B 端）→ `confirm_pairing` → `pairing_pin`（双端）→ `confirm_pairing` → `pairing_result`
- PIN 显示为大号数字，两端字体、分组（3+3）完全一致，方便目视比对
- 任一方拒绝或 60 秒超时 → "配对未完成，可以重新发起"

### 4.3 接收文件

对方发来文件时弹窗（除非开启自动接收）：

> **客厅电脑** 想把 **3 个文件（1.2 GB）** AA 给你
> 保存到：~/Downloads/AA4C（可更改）
> [接收] [拒绝]

调用 `accept_transfer(taskId, accept, saveDir)`。

### 4.4 传输完成

- 成功：系统通知 + 应用内 toast："已收到来自客厅电脑的 3 个文件"，点击打开文件夹
- 失败：toast 显示人话原因（见 §6），记录页可重试

## 5. 关键组件

### 5.1 DeviceCard 设备卡片

```
┌─────────────────┐
│   💻            │   ← 平台图标（💻🖥📱🗄）
│   客厅电脑       │   ← 设备名
│   ● 在线 · 已配对 │   ← 状态行
│   [ AA 文件 ]    │   ← 主操作（未配对时为 [配对]）
└─────────────────┘
```

状态组合：`在线/离线 × 已配对/未配对`。离线已配对设备仍显示（置灰），未配对且离线的设备不显示。

### 5.2 TransferCard 传输卡片

```
⬆ 发送至 客厅电脑                    取消 ✕
IMG_2024.jpg（3/5 个文件）
████████████░░░░░░░░  68% · 42 MB/s · 剩约 12 秒
```

- 进度条更新频率 ≤ 10次/秒（事件已节流）
- 速度显示自动换单位（KB/s / MB/s）；剩余时间用"剩约 X 秒/分钟"

### 5.3 全局任务条

有进行中任务时吸附在窗口底部，显示最活跃的一条 + 任务总数，点击展开任务列表。

## 6. 状态与文案规范

错误文案必须说**人话 + 给出下一步**：

| 内部错误（code） | UI 文案 |
|------------------|---------|
| `not_paired` | 还没有和这台设备配对，先配对一下吧 |
| `network` / 连接失败 | 连不上对方设备，请确认两台设备在同一个 WiFi |
| `transfer_rejected` | 对方拒绝了这次传输 |
| `hash_mismatch`（重试后仍失败） | 传输出了点问题，文件不完整，请重新发送 |
| `cancelled` | 已取消 |
| 磁盘空间不足（Io） | 空间不够了，清理一下磁盘或换个保存位置 |

通用规则：

- 不出现错误码、英文异常、堆栈
- 进行中状态用动词："正在寻找附近设备…"、"正在发送…"、"正在确认文件完整…"

## 7. 术语表（强制）

| ❌ 禁止出现 | ✅ 使用 |
|------------|--------|
| mDNS / 广播 / 发现协议 | 寻找附近设备 |
| 配对 / 密钥交换 / 证书 / 指纹 | 配对、确认码 |
| TLS / 加密握手 | （不提，默认安全；设置页可有一行"所有传输均已加密"） |
| 哈希校验 / BLAKE3 | 确认文件完整 |
| TCP / QUIC / 端口 | （不提） |
| P2P / 节点 | 设备 |
| Task / 任务队列 | 传输、记录 |

## 8. 视觉基调

- **风格**：简洁、留白多、圆角卡片；类 LocalSend / AirDrop 的轻量感
- **品牌色**：`#2F6BFF`（暂定，可后续品牌设计时替换；定义为 CSS 变量 `--aa-primary`）
- **深色模式**：跟随系统（Tauri `window.theme`），V0.1 必须支持
- **字体**：系统默认字体栈（PingFang SC / Microsoft YaHei / system-ui）
- **图标**：单色线性图标（lucide），平台图标可用 emoji 占位，V0.2 替换
- **最小窗口**：900 × 600；所有页面在该尺寸下不出现横向滚动

## 9. 前端状态管理（Pinia stores）

| Store | 状态 | 更新来源 |
|-------|------|----------|
| `useDeviceStore` | 本机信息、设备列表 | `list_devices` + `device_*` 事件 |
| `usePairingStore` | 进行中的配对会话、PIN | `pairing_*` 事件 |
| `useTransferStore` | 进行中任务、历史记录 | `list_transfers` + `transfer_*` 事件 |
| `useSettingsStore` | 设置项 | `get_settings` / `update_settings` |

事件监听在 App 根组件统一注册（`onMounted` 时 `listen('aa4c://...')`），分发给对应 store，组件只读 store。
