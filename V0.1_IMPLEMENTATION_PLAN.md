# AA4C V0.1 Implementation Plan

> 面向 AI Agent 的分步实现计划。**严格按里程碑顺序执行**，每个里程碑完成并通过验收标准后再进入下一个。
> 接口签名以 [API_DESIGN.md](API_DESIGN.md) 为准，表结构以 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md) 为准，UI 以 [UI_DESIGN_SPEC.md](UI_DESIGN_SPEC.md) 为准。

## V0.1 目标与验收（Definition of Done）

**目标：完成第一次 AA。**

最终验收场景（在两台真实设备或两个本机实例之间）：

1. 两台设备打开 AA4C，连接同一局域网，**10 秒内互相发现**
2. 通过 6 位 PIN 双向确认完成配对
3. A 拖拽一个文件夹（含 ≥1GB 大文件）到窗口，选择 B，点击 AA
4. B 弹窗确认接收，传输过程显示实时进度与速度
5. 传输完成，B 端文件 BLAKE3 哈希与源文件一致，目录结构保留
6. 双方"记录"页都能看到这条记录
7. 全程任意时刻可取消；杀掉一端进程，另一端在 10 秒内报告失败而非卡死

支持平台：Windows / macOS / Linux（CI 三平台构建通过）。

---

## 里程碑总览

| # | 里程碑 | 产出 | 预估 |
|---|--------|------|------|
| M0 | 工程脚手架 | Workspace + Tauri 可启动 + CI | 1 天 |
| M1 | 类型与存储 | `aa4c-types` + `aa4c-store` 完成 | 2 天 |
| M2 | 设备身份 | `aa4c-identity`：密钥、证书、TLS 配置 | 2 天 |
| M3 | 设备发现 | `aa4c-discovery`：mDNS 广播与浏览 | 2 天 |
| M4 | 配对协议 | `PairingManager` 端到端配对 | 3 天 |
| M5 | 传输引擎 | `aa4c-transfer`：发送/接收/校验/取消 | 5 天 |
| M6 | Core 组装 + Tauri 桥 | `aa4c-core` + Commands + Events | 3 天 |
| M7 | 前端 UI | 4 个页面 + 配对/接收弹窗 | 5 天 |
| M8 | 联调、打包、发布 | 三平台安装包 + v0.1.0 tag | 3 天 |

---

## M0 — 工程脚手架

**任务**：

1. 创建 Cargo workspace：`crates/{aa4c-types,aa4c-identity,aa4c-discovery,aa4c-transfer,aa4c-store,aa4c-core}`（先空实现 `lib.rs`）
2. `apps/desktop`：`pnpm create tauri-app`（Vue3 + TS 模板，Tauri 2），`src-tauri` 加入 workspace
3. workspace 级依赖统一在根 `Cargo.toml` 的 `[workspace.dependencies]` 声明（依赖清单见 API_DESIGN §11）
4. 日志：`tracing` + `tracing-subscriber`（env filter，默认 `info`）
5. CI（GitHub Actions）：三平台矩阵，跑 `cargo fmt --check`、`cargo clippy -- -D warnings`、`cargo test`、`pnpm build`
6. 添加 `LICENSE`（Apache-2.0）、`.gitignore`、`CHANGELOG.md`

**验收**：`cargo build` 全绿；`pnpm tauri dev` 打开空窗口；CI 三平台通过。

---

## M1 — 类型与存储

**任务**：

1. `aa4c-types`：按 API_DESIGN §3 实现全部类型、`Aa4cError`、`CoreEvent`（纯类型，禁止 I/O 依赖）
2. `aa4c-store`：
   - `migrations/001_init.sql`：按 DATABASE_SCHEMA §2 建 4 张表
   - 迁移执行器（`PRAGMA user_version`，见 DATABASE_SCHEMA §5）
   - 专职线程 + mpsc channel 封装 rusqlite 为 async 接口
   - 实现 API_DESIGN §7 的全部方法

**测试**：

- 迁移幂等性（打开两次不报错、版本正确）
- devices / transfer_tasks / settings 的 CRUD 往返
- 外键级联（删 device → 任务与文件明细被级联删除）

**验收**：`cargo test -p aa4c-store` 全绿。

---

## M2 — 设备身份

**任务**：

1. `Identity::load_or_generate`：Ed25519 密钥对生成、保存到 `<data_dir>/identity/`（私钥权限 0600，Windows 用默认 ACL）
2. `device_id = BLAKE3(public_key) hex`
3. `rcgen` 自签名证书（嵌入 Ed25519 公钥）
4. `tls_server_config` / `tls_client_config`：rustls 自定义证书校验器 —— **不走 CA**，校验"对端证书公钥指纹 == 期望的 DeviceId"（证书固定）
5. PIN 推导函数：`pin = u32::from_le_bytes(BLAKE3(min(pkA,pkB) || max(pkA,pkB))[0..4]) % 1_000_000`，格式化为 6 位补零字符串

**测试**：

- 两次 load 得到相同 device_id；删除目录后重新生成不同 id
- 自建 TLS server/client 互连：指纹匹配成功、指纹不匹配被拒绝
- PIN 双方计算结果一致且与公钥顺序无关

**验收**：`cargo test -p aa4c-identity` 全绿。

---

## M3 — 设备发现

**任务**：

1. `mdns-sd` 注册服务：类型 `_aa4c._tcp.local.`，TXT 含 `id/name/platform/ver/proto`
2. 浏览同类服务，解析 TXT → `DeviceInfo`，过滤自身
3. 设备列表维护：新发现 → `DeviceFound`；30 秒无响应 / 收到注销 → `DeviceLost`
4. 端口占用时自动递增重试（42420 → 42421 …），实际端口写进 mDNS

**测试**：

- 单元测试：TXT 解析、自身过滤
- 集成测试（单机起两个实例，不同端口）：A 能发现 B，B 停止后 A 收到 `DeviceLost`

**验收**：双实例互相发现；`cargo test -p aa4c-discovery` 全绿。

---

## M4 — 配对协议

**任务**：

1. 在 TLS 连接上实现配对消息（复用传输协议的帧格式：4 字节长度 + bincode）：`PairRequest / PairAccept / PairConfirm / PairReject`
2. `PairingManager`（API_DESIGN §4）：会话状态机 `Requested → PinShown → BothConfirmed → Done/Failed`
3. 配对期间 TLS 暂不校验指纹（首次见面），改为校验"证书公钥 == PairRequest 中声明的公钥"，PIN 双向确认替代 CA 信任
4. 60 秒超时、任一方拒绝 → 会话失败，事件通知
5. 成功后双方写入 `devices` 表（`trusted = 1`）

**测试**：

- 集成测试：双实例完整配对流程（自动确认 PIN）
- 拒绝路径、超时路径
- 配对后 `list_paired_devices` 双方都能查到对方

**验收**：双实例可编程完成配对；`cargo test -p aa4c-identity --test pairing` 全绿。

---

## M5 — 传输引擎（最大里程碑）

**任务**（按序提交）：

1. **帧编解码**：`Message` 的读写（长度前缀 + bincode），含模糊输入防御（长度上限 16 MiB）
2. **监听端**：TLS accept → 握手校验（Hello / 指纹 / trusted）→ 会话循环
3. **发送流程**：枚举文件（含递归文件夹，rel_path 规范化）→ 写 task 入库 → `Offer` → 等待 `OfferAnswer` → 逐文件分块发送（4 MiB），边读边算 BLAKE3 → `FileDone`
4. **接收流程**：`TransferRequest` 事件 → 等待 UI `accept()` → 落盘 `.aa4c-part`，边写边算哈希 → 校验 → 重命名 → `FileAck`
5. **路径安全**：拒绝 `..`、绝对路径、盘符；重名文件自动加后缀 `(1)`
6. **进度与取消**：进度事件 ≥100ms 节流；`CancellationToken` 贯穿读写循环；对端断连 → 任务 `Failed`
7. **哈希失败重传**：单文件最多重传 2 次
8. **持久化**：状态变更写库；进度 ≥1s 节流写库

**测试**：

- 单元：帧编解码 roundtrip、rel_path 规范化、路径穿越拒绝
- 集成（双实例 + 临时目录）：
  - 单文件、空文件、深层文件夹、1GB 大文件（哈希一致）
  - 拒绝接收、发送中取消、接收中取消
  - 模拟断连（直接 drop 连接）→ 双方任务 `Failed`
  - 篡改一个分块 → 哈希失败 → 重传成功

**验收**：`cargo test -p aa4c-transfer` 全绿；1GB 文件本机回环传输 ≥ 100 MB/s（基准参考，不阻塞验收）。

---

## M6 — Core 组装 + Tauri 桥

**任务**：

1. `aa4c-core`：`Core::start` 启动序列（identity → store → transfer listener → discovery），`shutdown` 逆序优雅关闭
2. broadcast 事件总线接通所有服务
3. 启动时清理遗留任务（`waiting_accept/transferring` → `failed`）
4. `src-tauri`：实现 API_DESIGN §9 全部 11 个 Command；`Core` 放入 Tauri `State`
5. 事件转发：`Core::subscribe()` → `app_handle.emit("aa4c://…")`，JSON camelCase
6. Command 错误统一映射为 `{ code, message }`

**测试**：Command 层薄，重点是一个端到端冒烟测试（Rust 侧直接调 Core：启动两实例 → 配对 → 传文件 → 断言记录）。

**验收**：`pnpm tauri dev` 启动后，在 devtools 里 `invoke('list_devices')` 能返回另一实例。

---

## M7 — 前端 UI

**任务**（按 UI_DESIGN_SPEC 实现）：

1. 项目基建：Vue Router（4 页）、Pinia stores（§9）、根组件统一注册事件监听
2. 布局：侧边导航 + 顶栏 + 全局任务条；深色模式跟随系统
3. 首页：本机卡片、设备网格（DeviceCard）、最近传输、空状态
4. AA 页：拖拽区（Tauri `onDragDrop`）、文件列表、设备选择、AA 按钮、进度视图（TransferCard）
5. 配对流程：发起、接受弹窗、PIN 大数字弹窗、结果 toast
6. 接收确认弹窗、系统通知（tauri-plugin-notification）
7. 记录页：分组列表、打开所在文件夹（tauri-plugin-opener）、失败重试
8. 设置页：4 个设置项 + 已配对设备管理
9. 全部文案过一遍 §7 术语表，禁词检查

**验收**：手动走通最终验收场景 1–7；`pnpm build` 无 TS 错误。

---

## M8 — 联调、打包、发布

**任务**：

1. 双真机联调（至少 macOS ↔ Windows 一组），修联调 bug
2. 防火墙提示文档：首次启动各平台的放行说明写入 README
3. `tauri build` 三平台产物：`.dmg` / `.msi` / `.AppImage`（CI release workflow，tag 触发）
4. 补 `CHANGELOG.md`，打 `v0.1.0` tag，GitHub Release

**验收**：三平台安装包可安装运行，完成一次跨平台真实 AA。

---

## 风险与注意事项

| 风险 | 对策 |
|------|------|
| mDNS 在某些路由器/虚拟网卡环境失效 | 记录排查文档；V0.1 接受此限制，UDP 广播兜底放 V0.2 备选 |
| Windows 防火墙默认拦截入站 | 安装包声明防火墙规则；UI 在"找不到设备"空状态里提示检查防火墙 |
| 大文件传输内存占用 | 严格流式读写，任何时刻内存中不超过 2 个 chunk |
| rustls 证书固定的自定义校验器易写错 | M2 必须有正反两个测试（匹配通过 / 不匹配拒绝） |
| 双实例集成测试端口冲突 | 测试一律用端口 0（系统分配）+ 临时数据目录 |

## 范围外（明确不做，防止蔓延）

- ❌ 断点续传恢复（重启后续传）—— V0.2
- ❌ 同步、分享、下载、AI —— 各自版本
- ❌ 移动端 —— V0.2
- ❌ 广域网 / NAT 穿透 —— V0.3
- ❌ 多语言 i18n —— V0.1 只做简体中文，但文案集中在一个文件中便于后续提取
