# AA4C V0.5 Implementation Plan（AI 归档）

> 依据 [ARCHIVE_DESIGN.md](ARCHIVE_DESIGN.md)（v1 定稿；其 §10 是已确认决策，**不要重开已定案的讨论**；§11 列了实现期必须补的实证项）。
> 给执行 Agent：开工前先读 [AGENTS.md](AGENTS.md)（工程约定）、[HANDOFF.md](HANDOFF.md)（环境坑 + 历届教训，尤其第五节）、本文下方「执行纪律」。
> 每个里程碑独立提交、独立验收；提交前自检 `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`（`aa4c-core --test core` 在并行下有已知的既有 flaky，判定标准是**单线程重跑全绿**，见 HANDOFF.md）+ 前端改动时 `pnpm build`。接口/表结构变更**先改文档**（DATABASE_SCHEMA / API_DESIGN / ARCHIVE_DESIGN）再写代码。
> **V0.5 不碰设备间线路协议**：`PROTO_VERSION` 不动、`aa4c-proto` 不动——归档/AI 是纯本机能力。与旧版本客户端天然互通，无兼容性注意事项。

## 执行纪律（本项目血泪教训的浓缩，违反其中任何一条都曾真实付出过代价）

1. **外部事实必须实证，不信二手文档**：涉及外部组件的行为（llama-server 参数、release 产物内容、模型文件格式），动手前在真机/真实 CI 上验证一次。先例：aria2"官方有二进制"对 2/3 平台不成立（D1）；Transmission 配置键名与官方 RPC 文档不一致（D3）；网页摘要声称的 snake_case 键名实测为假（D3）。
2. **报错语焉不详时立刻上 verbose/看底层输出，不要连猜两个"看起来合理"的原因**（AppImage 教训：FUSE 和 strip 两个假设都错，`--verbose` 一次就照出真因）。
3. **测试用真实进程/真实文件，不 mock**：`aa4c-download` 的测试起真 aria2c/transmission-daemon，`aa4c-ai` 同样起真 llama-server（CI 用 ≈1MB 微型 GGUF，见 AI2.0）。
4. **最小改动面**：能复用既有先例（错误处理、事件形态、UI 组件、测试构造法）就复用，不发明新模式。每个设计决定在 ARCHIVE_DESIGN §10 都有落点，照做。
5. **危险操作显式确认**：本版本唯一动用户文件的路径是"移动"——严格按 ARCHIVE_DESIGN §2.4 的触发范围执行，不扩大。
6. 完成每个里程碑后照例更新 CHANGELOG / HANDOFF（进度表 + 下一步）/ ARCHIVE_DESIGN 的「已实现/实现偏差」标注。

## V0.5 目标与验收（Definition of Done）

在桌面三平台上：

1. 下载一个 `.gguf` 模型文件 → 自动识别为模型（架构/量化等元数据正确展示）→ 按启用的规则自动移入模型目录 → 模型库可见、可设为对话模型；
2. 归档动作全程有记录、可撤销；文件在同步范围内时有明确警示；
3. 配好本地模型后，对一批文件请求 AI 标签/分类建议，采纳后生效——AI 从不擅自动文件；
4. 把自己的笔记目录加入知识库，提问得到带来源引用的流式回答；
5. 全程零云端调用；引擎/模型未配置时一切非 AI 功能完整可用，AI 功能优雅降级为引导配置；
6. 用户不需要安装配置任何外部工具（llama-server 随安装包分发；模型经应用内推荐卡片一键下载）。

## 里程碑总览

| 里程碑 | 内容 | 前置 | 交付判定 |
|--------|------|------|----------|
| AI1 ✅ 已实现 | 规则式归档引擎 + 归档页 v1（无 AI） | — | 真实下载 .gguf 自动归位；撤销生效；警示可见 |
| AI2 ✅ 代码/CI 已实现，AppImage 待发版验证 | `aa4c-engine` 重构 + llama-server 接入 + 打包腿 + 模型库 | AI1 | 真机模型加载就绪（已达成）；三平台安装包含引擎（AppImage 实证，代码就绪、留给 AI5 发版验证） |
| AI3 ✅ 已实现 | AI 标签/分类建议 | AI2 | 真实模型批量建议 → 采纳生效（已用真实 llama-server + 微型 GGUF 走通 `Core` 全链路，含撤销） |
| AI4 | 本地知识库 | AI2 | 真实笔记目录问答带引用 |
| AI5 | 收尾 + `v0.5.0-preview` 发布 | AI1–AI4 | 三平台 + Android 产物齐全 |

---

## AI1 — 规则式归档引擎（细化到步骤）

1. **AI1.1 文档先行**：DATABASE_SCHEMA.md 新增 §4f（迁移 009 四张表 + Settings 新 KV 键，内容照 ARCHIVE_DESIGN §4）；API_DESIGN.md 命令表追加 AI1 的 6 个 Command（见 AI1.6）；UI_DESIGN_SPEC.md 补归档页文案要点。
2. **AI1.2 `aa4c-store`**：迁移 `009_archive.sql`（照 ARCHIVE_DESIGN §4 原文）；CRUD 方法：规则（list/upsert/delete，含 position 排序）、entries（insert/list/get/update_path）、tags（set/list）、log（append/list/mark_undone）；`update_download_save_path(id, new_path)`。每个方法配真实 SQLite 测试（同 `store.rs` 既有风格）。
3. **AI1.3 识别与 GGUF**（`aa4c-core::archive::detect` / `archive::gguf`）：类别表 + ~20 条 magic；GGUF 头解析器严格按 ARCHIVE_DESIGN §2.2（含硬界限）。单测：手工构造的最小 GGUF 字节 + 越界样本 + 各 magic 样本。
4. **AI1.4 规则引擎**（`aa4c-core::archive`）：匹配（第一条命中）、模板展开（占位符缺值 → `未知`）、移动（rename→EXDEV 回退；同名加序号复用 `unified.rs` 逻辑）、写 entries/log、更新 save_path、发 `CoreEvent::ArchiveApplied`；撤销。首次启动写入五条**停用**的预设规则。
5. **AI1.5 下载完成钩子**：`Core::start` 里起后台任务订阅自身事件总线（照 `sync_exchange` 循环的先例），`DownloadDone` 且 `archive_auto_enabled` → 跑规则。**只挂这一个事件**。
6. **AI1.6 编排 + Command**（orchestrate.rs 薄转发 + commands.rs + generate_handler 注册，D3 先例）：`list_archive_rules` / `save_archive_rule` / `delete_archive_rule` / `list_archive_entries` / `archive_files`（手动，含目标覆写）/ `undo_archive`。`Settings` 增 `archive_root` / `archive_auto_enabled`（aa4c-types 4 处惯例改法：struct/序列化测试/core settings 常量/load+save）。
7. **AI1.7 前端**：ArchivePage 替换占位（归纳 + 规则两个分区）；设置页「归档」区块（目录选择 + 同步范围警示，**照抄 D3 SettingsPage 下载目录警示的实现**）；types/api/store 惯例三件套。
8. **AI1.8 测试 + 收尾**：`aa4c-core` 集成测试——真实临时目录里走"假下载完成事件 → 规则移动 → 撤销"全链路；全量验证 + 文档标注。

## AI2 — AI 引擎接入（细化到步骤）

0. **AI2.0 前置实证（先做，产出写进 ARCHIVE_DESIGN §3.1/修订，再动代码）**：锁 llama.cpp tag；三平台产物下载、列内容、`otool -L`/`ldd` 出裁剪清单；`--help` 确认 JSON Schema 请求形态与 `--pooling`；微型 GGUF（tinyllamas stories260K ≈1MB）上传 `engines/test-fixtures` release 并记 SHA256；本机用微型模型真实跑通 `llama-server` `/health`+`/v1/chat/completions`+`/v1/embeddings`。
1. **AI2.1 `aa4c-engine` 重构**：`SidecarSpawner`/`EngineChild`/`ProcessSpawner`/`orphan_guard` 平移出 `aa4c-download`；download 改 re-export 或改 import；**全部既有测试零修改通过 = 重构完成判据**；桌面 `TauriSidecarSpawner` 只改 import。独立 commit。
2. **AI2.2 `aa4c-ai` crate**：`LlamaProcess`（env 配置 spawn + 健康轮询 120s + 优雅退出）、手写 HTTP 客户端（`TransmissionClient` 先例 + SSE 流解析）、`AiService`（双槽位、懒启动、空闲自停、`AiEngineState` 事件、`Unavailable` 语义）。集成测试用微型模型走真实进程全链路。
3. **AI2.3 打包腿**：engines.yml llama 腿（下载官方产物→lipo universal→裁剪→SHA256→发 `engines/llama-<tag>`）；`fetch-engines.sh` 学会 llama 形态（+`--from-path`）；`tauri.conf.json` 第三 externalBin + resources；`ci.yml` 三平台装 llama-server；`release.yml` ubuntu job 处理 AppImage 的 `LD_LIBRARY_PATH`（ARCHIVE_DESIGN §3.4 预警）——**真实 CI 验证 AppImage 产出后才算完**；Android 哨兵跑一次。
4. **AI2.4 模型库**：`Settings` 增 `ai_models_dir`/`ai_chat_model`/`ai_embedding_model`/`ai_idle_timeout_minutes`；Command：`list_local_models`（扫描+GGUF 元数据）/`get_ai_status`；前端模型库分区（列表/选定/状态/推荐卡片双源直链——URL 此刻核实）+ 设置页「AI」区块。
5. **AI2.5 测试 + 收尾**：全量验证 + 文档；HANDOFF 记录 AI2.0 实证结论。

## AI3 — AI 标签/分类建议

1. `aa4c-ai::suggest`：输入构造（文件名+类别+文本头 8KB）、JSON Schema 约束请求、低温采样;批量队列（单并发、逐个、失败跳过）+ `AiSuggestProgress` 事件；建议存内存态。
2. 编排 + Command：`start_suggest`（文件列表）/`list_suggestions`/`resolve_suggestion`（采纳=写标签+可选移动，走 AI1 的既有归档动作；忽略=丢弃）。
3. 前端：归纳分区加"待确认"列表（逐条/批量采纳、忽略）+ 进度显示 + 引擎加载态提示。
4. 集成测试：微型模型真实出建议（**断言 JSON 结构合法与流程完整，不断言建议内容质量**——微型模型说胡话是预期内的）。

## AI4 — 本地知识库

1. `aa4c-store` 迁移 `010_knowledge.sql`（照 ARCHIVE_DESIGN §4）+ CRUD；DATABASE_SCHEMA 同步。
2. `aa4c-ai::kb`：扫描（mtime+hash 增量）、分块（~1000 字符/重叠 200/段落优先）、批量嵌入、暴力余弦 top-k、问答 prompt + SSE 流式 → `KbAnswerDelta`/`KbAnswerDone` 事件；摄入可中断；5 万 chunk 警告。
3. Command：`kb_add_source`/`kb_remove_source`/`kb_list_sources`/`kb_reindex`/`kb_ask`。
4. 前端：知识库分区（来源管理、摄入进度、问答框流式渲染、来源引用可点开所在文件夹）。
5. 集成测试：微型模型 + 临时目录几个 md 文件 → 摄入 → 提问 → 断言引用命中正确文件（内容质量不断言）。

## AI5 — 收尾 + 发布

1. 全量验证（三平台 CI 绿 + 本地单线程复核）；真机 `pnpm tauri dev` 走查 DoD 六条。
2. 文档终态：ARCHIVE_DESIGN 标已实现+实现偏差；CHANGELOG `[Unreleased]`→`[0.5.0-preview]`；HANDOFF 进度表/头部/下一步；README 能力清单；ROADMAP V0.5 标完成。
3. 工作区版本号 0.4.0→0.5.0（Cargo.toml/tauri.conf.json/package.json 三处）；tag `v0.5.0-preview` 触发 release.yml；确认 5 个 job 全绿、产物齐全（**含 AppImage**）。

## 关键文件一览

| 文件 | 改动 |
|------|------|
| `ARCHIVE_DESIGN.md` / `DATABASE_SCHEMA.md` / `API_DESIGN.md` / `UI_DESIGN_SPEC.md` | 设计与契约（每里程碑先行） |
| `crates/aa4c-store`（迁移 009/010 + CRUD） | AI1/AI4 |
| `crates/aa4c-core/src/archive/`（detect/gguf/规则引擎/钩子） | AI1 |
| `crates/aa4c-engine`（新，sidecar 设施平移） | AI2.1 |
| `crates/aa4c-ai`（新：process/client/service/suggest/kb） | AI2–AI4 |
| `crates/aa4c-types`（Settings 新字段、新事件） | AI1/AI2 |
| `crates/aa4c-core/src/{orchestrate,lib,settings}.rs` + `apps/desktop/src-tauri/src/{commands,lib}.rs` | 各里程碑编排/Command |
| `.github/workflows/{engines,ci,release}.yml` + `scripts/fetch-engines.sh` + `tauri.conf.json` | AI2.3 |
| `apps/desktop/src/pages/{ArchivePage,SettingsPage}.vue` + `lib/{types,api}.ts` + `stores/archive.ts` | AI1–AI4 前端 |
