# Security Policy

## 报告漏洞

**请不要为安全漏洞开公开 Issue。**

请通过以下方式私下报告：

1. **首选**：GitHub Security Advisory —— 仓库页面 → Security → [Report a vulnerability](https://github.com/HuoTaoCN/AA4C/security/advisories/new)
2. 或发送邮件至：security@aa4c.com

报告请尽量包含：影响版本、复现步骤、影响评估（能读什么/写什么/冒充谁）、可能的修复建议。

响应承诺：

| 阶段 | 时限 |
|------|------|
| 确认收到 | 72 小时内 |
| 初步评估 | 7 天内 |
| 修复与披露 | 视严重程度，目标 90 天内；修复发布后公开致谢（除非你希望匿名） |

## 支持版本

| 版本 | 支持状态 |
|------|----------|
| 最新 release | ✅ 接收安全修复 |
| 更早版本 | ❌ 请升级 |

## 安全设计概览

AA4C 的安全模型（详细协议见 [PROTOCOL.md](PROTOCOL.md)）：

| 层 | 机制 |
|----|------|
| 设备身份 | Ed25519 密钥对；DeviceId = BLAKE3(公钥)，不可伪造 |
| 信任建立 | 双向 6 位 PIN 目视确认（短认证串），PIN 两端独立计算、不经网络传输 |
| 通道加密 | TLS 1.3，自签名证书 + 证书固定（指纹 = DeviceId），不依赖 CA |
| 数据完整性 | 文件级 BLAKE3 哈希校验，失败自动重传 |
| 授权 | 只有已配对（trusted）设备可发起传输；每次接收需用户确认（可对信任设备开启自动接收） |
| 密钥保管 | 私钥仅存本地（文件权限 0600），永不入库、不入日志、不离开设备 |

## 威胁模型

### 在防护范围内

| 威胁 | 对策 |
|------|------|
| 局域网窃听 | 全程 TLS 1.3 加密 |
| 中间人（MITM） | 证书固定；配对期靠双向 PIN（攻击者无法令两端 PIN 一致） |
| 伪造设备身份 | DeviceId 绑定公钥哈希，无私钥无法完成 TLS 握手 |
| 恶意文件路径（路径穿越） | 接收端强制净化 rel_path，拒绝 `..` / 绝对路径 |
| 未配对设备发起传输 | 握手即校验 trusted，拒绝并断开 |
| 传输内容篡改 | BLAKE3 校验 |
| 协议层 DoS（超长帧等） | 帧长上限 16 MiB，非法帧立即断开 |
| （V0.3+）Rendezvous/Relay 服务器读取内容 | 端到端加密，服务器只见密文与端点映射 |

### 不在防护范围内（明确非目标）

- 设备本身被攻破（恶意软件、root 后的系统）
- 用户主动配对了攻击者的设备（PIN 核对被用户跳过）
- 流量分析（观察者可知"两台设备在传输"及流量大小，但不知内容）
- 物理访问已解锁的设备

## 给贡献者的安全规则

- 任何涉及密钥、TLS 校验、路径处理的代码改动，PR 必须包含正反两向测试（合法通过 / 非法拒绝）
- 禁止在日志中输出：私钥、完整公钥、文件内容、用户路径以外的敏感信息
- 新增依赖前检查其安全公告史；CI 中运行 `cargo audit`
- 不引入"关闭加密/校验"的配置项 —— 安全特性不可配置关闭
