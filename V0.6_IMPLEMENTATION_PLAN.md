# AA4C V0.6 Implementation Plan（Touch/Direct）

> 依据 [TOUCH_DESIGN.md](TOUCH_DESIGN.md)（v1 设计稿；其 §9 是已确认决策，**不要重开已定案的讨论**；§10 列了实现期必须补的实证项）。
> 给执行 Agent：开工前先读 [AGENTS.md](AGENTS.md)（工程约定）、[HANDOFF.md](HANDOFF.md)（环境坑 + 历届教训，尤其第五节）、本文下方「执行纪律」。
> 每个里程碑独立提交、独立验收；提交前自检 `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace` + 前端改动时 `pnpm build`。接口/表结构变更**先改文档**（DATABASE_SCHEMA / API_DESIGN / TOUCH_DESIGN）再写代码。
> **V0.6 会碰设备间线路协议**：`aa4c_types::CoreEvent::ConnectionVia` 新增变体（只追加）；`aa4c-proto::Message` 不新增字段——AA Touch 的配对令牌复用既有 `PairRequest`/`PairAccept` 流程内部的会话机制，不新造消息类型（具体复用哪个字段是 T1 的第一个实证任务，见执行纪律第 1 条）。

## 执行纪律（本项目血泪教训的浓缩 + V0.6 特有的一条新纪律）

1. **外部事实必须实证，不信二手文档**：TOUCH_DESIGN.md §1 的平台事实已经用官方文档源核实过一轮，但那是"网页文档层面"的核实，不是"真实硬件层面"的核实——**每个里程碑动手写业务代码之前，必须先有一个跑通的最小可行 demo**（见 TOUCH_DESIGN.md §10），这是本项目一贯的"先证明技术路径可行，再写业务代码"纪律（同 AI2.0 先例）的延伸。
2. **V0.6 特有纪律：本环境（沙箱开发机）没有任何 NFC/WiFi Direct/蓝牙硬件，也没有真实 Android 设备——这条纪律在 V0.6 之前的所有里程碑都不成立，必须认识到执行模式变了**。之前的里程碑（D1-D3/AI1-AI5）里，Agent 自己起真实进程/真实文件就能验证到"功能确实工作"这个程度；V0.6 里，Agent 只能验证到"代码编译通过、单元测试覆盖了纯逻辑部分、没有明显的类型/逻辑错误"，**无法自己验证"碰一碰真的能配对""WiFi Direct 真的能连上"这类核心功能是否成立**。每个里程碑的验收判定里明确写了"需要用户用真实设备验证"的部分，Agent 完成编码后必须如实说明"这部分我验证不了，需要你用真机测"，不能含糊带过、更不能编造"已验证"。
3. **报错语焉不详时立刻上 verbose/看底层输出，不要连猜两个"看起来合理"的原因**（AppImage 教训：FUSE 和 strip 两个假设都错，`--verbose` 一次就照出真因）。
4. **最小改动面**：能复用既有先例（`PunchDialer`/`RelayDialer` 的 dialer 注入模式、`ConnectionVia` 加变体的方式、`MainActivity.kt` 定制 vs 正式 Tauri 插件的取舍）就复用，不发明新模式。每个设计决定在 TOUCH_DESIGN §9 都有落点，照做。
5. **危险操作显式确认**：NFC/蓝牙广播涉及主动暴露设备信息，严格按 TOUCH_DESIGN §6 的"仅用户主动触发+有限时间窗口"执行，不做成常驻后台服务。
6. 完成每个里程碑后照例更新 CHANGELOG / HANDOFF（进度表 + 下一步）/ TOUCH_DESIGN 的「已实现/实现偏差」标注——**并且如实标注"哪些验收项还没有真机验证过"**，不要因为代码写完了就默认标"已实现"。

## V0.6 目标与验收（Definition of Done）

1. 两台 Android 设备互相碰一下，无需手动输入 PIN 即可完成配对（TOUCH_DESIGN §2）；
2. 两台 Android 设备在没有共同 WiFi、没有互联网的环境下，仍能建立连接并互传文件（TOUCH_DESIGN §3）；
3. 至少一台桌面设备能通过蓝牙发现并连接一台正在广播的 Android 设备，完成小文件传输（TOUCH_DESIGN §4）；
4. AA Touch/AA Direct 均为可选开关，关闭时不影响任何既有功能（局域网直连/远程/分享/下载/归档/AI 全部照常工作）；
5. 全程不新增用户需要手动安装配置的外部工具或依赖；
6. **以上 1-3 条的验收必须由用户在真实设备上确认**——Agent 自测只能到"编译通过 + 单元测试覆盖纯逻辑 + 最小可行 demo 跑通"这个程度（见执行纪律第 2 条）。

## 里程碑总览

| 里程碑 | 内容 | 前置 | 交付判定 |
|--------|------|------|----------|
| T1 | AA Touch（Android HCE 自定义插件 + 官方 NFC 插件读取 + 配对流程接线） | — | 两台真机碰一碰完成配对（用户验证） |
| T2 | AA Direct WiFi Direct（Android） | T1 的配对令牌机制 | 两台真机断网互传文件（用户验证） |
| T3 | AA Direct 蓝牙（桌面 central + Android 双角色） | — | 真机蓝牙发现+小文件传输（用户验证） |
| T4 | 收尾：全量验证 + 文档 + `v0.6.0-preview` 发布 | T1–T3 | 三平台 + Android 产物齐全 |

---

## T1 — AA Touch（细化到步骤）

0. **T1.0 前置实证（先做，产出写进 TOUCH_DESIGN §1.1/§10 修订，再动代码）**：
   - 查 Tauri 官方移动插件开发文档，确认自定义 Android 插件的标准挂接方式（`tauri::plugin::Builder` + Kotlin `Plugin` 子类 + `@Command` 方法），本项目至今没写过正式插件，`MainActivity.kt` 里的 `MulticastLock` 定制走的是更简单的生命周期钩子，不适用于需要接收系统 `HostApduService` Intent 的场景。
   - 写一个最小 `HostApduService` demo：广播一段固定字符串，用真实 Android 设备 + 官方 `tauri-plugin-nfc` 的 `scan()` 验证能读到（需要用户配合，两台手机或一台手机+一张可编程 NFC 卡模拟）。
   - 核对 `aa4c-identity::pairing` 现有配对会话内部的令牌/密钥材料结构，确定 NFC 广播的 payload 具体传什么字段，不新造一套。
1. **T1.1 Android HCE 插件**：`HostApduService` 子类 + AID 路由注册（`res/xml/apduservice.xml`）+ 广播开始/停止的生命周期管理（对应 TOUCH_DESIGN §6 的"有限时间窗口"）。
2. **T1.2 配对流程接线**：`PairingManager` 新增"NFC 广播"与"NFC 读取"两个分支，复用既有 `PairRequest`/`PairAccept` 握手；`Core` 新增 Tauri Command 触发广播/读取；`CoreEvent` 新增 NFC 相关事件（具体命名实现期定，同 `PairingPin`/`PairingResult` 的既有命名风格）。
3. **T1.3 前端**：配对页新增"碰一碰配对"入口；设置页新增开关（TOUCH_DESIGN §7）。
4. **T1.4 测试 + 收尾**：纯逻辑部分（令牌生成/校验/超时）单元测试；`cargo fmt`/`clippy`/`test --workspace`/`pnpm build` 全绿；**明确标注"HCE 广播-读取真实闭环、配对完整流程"未经真机验证，需要用户测试**，更新 CHANGELOG/HANDOFF/TOUCH_DESIGN。

## T2 — AA Direct WiFi Direct（细化到步骤）

0. **T2.0 前置实证**：`WifiP2pManager` 最小可行 demo——两台真实 Android 设备真实建立 P2P 群组、互传一个文件（需要用户配合），记录真实需要哪些权限弹窗、Group Owner 协商成功率、实测吞吐量，写进 TOUCH_DESIGN §1.2/§10 修订。
1. **T2.1 `aa4c-transfer::WifiDirectDialer`**：trait 定义仿 `PunchDialer`/`RelayDialer`；桌面壳层返回"不可用"；Android 壳层实现真正调用。
2. **T2.2 连接阶梯接线**：`ConnectionVia` 新增 `WifiDirect` 变体；`dial()` 在既有四档失败后、仅 Android 平台尝试这一档。
3. **T2.3 前端**：连接质量徽标新增"脱网直连"展示（同 C4 先例）。
4. **T2.4 测试 + 收尾**：dialer trait 的纯逻辑部分（重试/超时/回退判断）单元测试；**明确标注真实 P2P 建连/断网场景传输未经真机验证**，更新文档。

## T3 — AA Direct 蓝牙（细化到步骤）

0. **T3.0 前置实证**：`btleplug` 桌面 central 最小可行 demo（真实扫描到一个 BLE 广播源、建立 GATT 连接、读写特征值）+ Android `BluetoothLeAdvertiser`/`BluetoothGattServer` 最小可行 demo（真实广播+被连接），两端各自独立验证后再验证互通（需要用户配合真实硬件）。
1. **T3.1 `aa4c-transfer::BleDialer`**：trait 定义仿既有 dialer；桌面壳层基于 `btleplug`（仅 central）；Android 壳层原生实现（central + peripheral）。
2. **T3.2 小文件传输路径**：GATT 特征值分片传输（MTU 协商、重组、校验）；Android-Android 场景下握手成功后尝试升级 WiFi Direct（TOUCH_DESIGN §1.3/§4）。
3. **T3.3 前端**：连接质量徽标新增"蓝牙（较慢）"展示。
4. **T3.4 测试 + 收尾**：分片/重组逻辑单元测试（用假数据，不需要真实蓝牙）；**明确标注真实蓝牙发现/连接/传输未经真机验证**，更新文档。

## T4 — 收尾 + 发布

1. 全量验证（三平台 CI 绿 + 本地单线程复核）；**真机走查交给用户**——本环境做不了，Agent 只能确认代码/CI 层面全绿。
2. 文档终态：TOUCH_DESIGN 标已实现/实现偏差（**如实区分"代码已实现"与"真机验证通过"两个状态，不要混为一谈**）；CHANGELOG `[Unreleased]`→`[0.6.0-preview]`；HANDOFF 进度表/头部/下一步；README 能力清单；ROADMAP V0.6 标完成。
3. 工作区版本号 0.5.0→0.6.0；tag `v0.6.0-preview` 触发 release.yml（**打 tag/推送是公开发布动作，同既往先例需要用户明确指示后再执行**）。

## 关键文件一览

| 文件 | 改动 |
|------|------|
| `TOUCH_DESIGN.md` / `DATABASE_SCHEMA.md`（如需要）/ `API_DESIGN.md` | 设计与契约（每里程碑先行） |
| `crates/aa4c-transfer/src/lib.rs`（新增 `WifiDirectDialer`/`BleDialer` trait） | T2/T3 |
| `crates/aa4c-identity/src/pairing.rs`（NFC 令牌接线） | T1 |
| `crates/aa4c-types`（`ConnectionVia` 新变体、`Settings` 新字段、新事件） | T1/T2/T3 |
| `apps/desktop/src-tauri/gen/android`（自定义 HCE 插件、`WifiP2pManager`/蓝牙原生实现） | T1/T2/T3 |
| `apps/desktop/src-tauri/src/{commands,lib}.rs` + `crates/aa4c-core/src/orchestrate.rs` | 各里程碑编排/Command |
| `apps/desktop/src/pages/{SettingsPage,SendPage 或新配对页}.vue` + `lib/{types,api}.ts` | 前端 |
