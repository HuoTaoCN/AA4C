# AA连接（AA4C）产品需求与技术架构白皮书（V1.2）

## 一、项目定义

- **品牌名称**：AA连接
- **英文名称**：AA4C（**AA for Connection**，即"AA连接"）
- **域名**：aa4c.com
- **开源协议**：Apache License 2.0

### 项目愿景

> **让所有设备直接连接。**

AA连接**不是**下载器、BT 工具、云盘、网盘、同步工具，也不是社区平台。它的本质是：

> **一个开源的跨平台设备连接平台（Device Connection Platform）。**

文件传输只是设备连接后的**第一种能力**；连接建立后，同步、分享、下载、AI 归档、自动协同等能力都自然发生。但核心永远是**设备与设备的连接**。

- **Slogan（首选）**：连接你的所有设备。
- **Slogan（备选）**：让所有设备成为一个空间。
- **一句话传播**：把文件 AA 给我。

用户无需理解云盘、FTP、SMB、BT、P2P、NAT、同步协议，只需要知道：连接设备、把文件 AA 给我。

## 二、核心理念

### AA

AA 是动作，代表：发送、分享、同步、归档、分发。

例如：

- 把照片 AA 给我
- 把模型 AA 给我
- 把文件夹 AA 一下

AA 是整个产品体系中的核心交互语言。

### 4C = for Connection

**AA4C = AA for Connection = AA连接。** "4C" 取 "for Connection"（4≈for、C=Connection）——一切围绕"连接"展开。

### 核心思想：连接优先

传统软件关注"文件 → 传输"；AA连接关注：

```text
设备 → 连接 → 能力
```

先把设备连起来，能力（发送、同步、分享、下载、AI 归档、自动协同）在连接之上自然生长。**连接优先，功能其次。**

## 三、产品定位

### 用户定位

AA连接面向**个人 / 家庭 / 小团队**：

| 对象 | 典型需求 |
|------|----------|
| 个人用户 | 手机 / 电脑 / 平板 / NAS / 服务器 多设备互联互通 |
| 家庭用户 | 家庭照片、视频、资料的多设备同步与共享 |
| 小团队 | 指定成员之间的文件同步、分享、项目协同 |

### 产品定位

一个**开源的跨平台设备连接平台**——通过连接设备，让文件传输、同步、分享、下载、归档等能力自然发生。

### 产品边界（不做）

| 不做 | 原因 |
|------|------|
| 社区 / 内容平台 | 内容审核、举报处理、法律风险、用户运营、服务器投入成本极高，非本项目目标 |
| 资源平台（资源广场 / 模型广场 / 软件商店 / 文件社区） | 不做中心化内容分发 |
| 中心化云盘 / 网盘 | 坚持**用户数据属于用户自己**，不建集中存储 |

AA Share 的"分享"是**指定好友 / 指定设备**之间的分享，**不是社区分享**。

## 四、核心用户场景

| 场景 | 描述 |
|------|------|
| **家庭** | 父母手机照片自动同步到家庭电脑 / NAS，形成家庭相册 |
| **个人多设备** | Mac、Windows、Linux、Android、iPhone 之间自动同步 |
| **朋友聚会** | 连接同一 WiFi 即可发现设备，互相发送照片、视频、文件 |
| **远程传输** | 重庆 ↔ 北京 ↔ 上海 ↔ 海外，设备之间直接传输，无需云盘 |
| **模型与知识库** | 下载 GGUF 模型后自动同步到 NAS 和工作站，自动归档 |

## 五、产品核心能力

### 1. AA Send（文件传输）

- 局域网发现、局域网直连、跨平台、多设备
- 拖拽发送、二维码发送、文件夹发送
- 支持：图片、视频、文档、压缩包、模型文件等任意文件

### 2. AA Sync（文件同步）

- **核心能力**：持续同步、增量同步、多设备同步
- **同步模式**：单向同步、双向同步、镜像同步、选择性同步
- **同步对象**：文件夹、照片库、知识库、模型库、项目目录
- **元数据优先 + 按需获取（V0.2 首版）**：默认只在自己的设备间同步文件名/目录（轻量），内容点了才拉；每个文件按可获取性着色——🟢 本地已有 / 🟡 在线设备有可下载 / 🔴 仅离线设备有暂不可取。详见 [SYNC_DESIGN.md](SYNC_DESIGN.md)。

### 3. AA Share（远程分享）

- **分享对象**：好友、家庭、团队、社区
- **分享方式**：临时分享、长期分享、只读分享、读写分享

### 4. AA Download（下载中心）

- **协议**：HTTP、HTTPS、FTP、S3、BT、Magnet
- **统一任务管理**：所有下载在一个任务中心管理

### 5. AA AI（智能归档）

- **自动识别**：图片、视频、文档、代码、模型、压缩包
- **自动处理**：自动分类、自动标签、自动归档

例如：下载 `Qwen3-35B.gguf` → 自动识别为 AI 模型 → 自动移动到 `/Models/Qwen/`。

### 连接优先：技术演进路线

能力围绕"连接方式"分阶段演进（连接优先、功能其次）：

| 阶段 | 名称 | 连接方式 | 目标 |
|------|------|----------|------|
| 第一阶段 | **AA Nearby** 近场连接 | 局域网 / WiFi：自动发现、自动连接、文件传输 | 替代 LocalSend |
| 第二阶段 | **AA Sync** 同步系统 | 文件夹同步、自动同步、增量同步 | 替代 Syncthing |
| 第三阶段 | **AA Connect** 远程连接 | NAT 穿透、P2P、Relay，跨互联网连接设备 | 突破局域网 |
| 第四阶段 | **AA Touch** 碰一碰 | NFC：设备 A 碰设备 B → 自动连接 | 极简配对 |
| 第五阶段 | **AA Direct** 脱网连接 | WiFi Direct、蓝牙、蓝牙 Mesh | 无互联网、无基站也能连 |

**未来方向：Device-to-Device（D2D）设备直连**——手机↔手机、手机↔电脑、电脑↔NAS、手机↔车机、手机↔无人机；随 4G/5G/6G 的设备直连能力增强持续演进。

## 六、产品结构

| 页面 | 内容 |
|------|------|
| **首页** | 设备状态、同步状态、最近文件、最近设备、任务状态 |
| **AA（发送）** | 发现设备、选择文件、发送 |
| **Sync** | 同步目录管理、同步状态、同步策略 |
| **Share** | 生成分享、管理分享、查看访问记录 |
| **Download** | 下载任务、BT 任务、历史记录 |
| **AI** | 归档中心、标签管理、分类规则、知识库 |

## 七、技术架构

总体分层：

```
AA4C Core → Plugin System → Transport Layer → Storage Layer
```

详细架构见 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 八、技术选型

| 层 | 技术 | 理由 |
|----|------|------|
| 核心语言 | **Rust** | 性能高、跨平台、内存安全、生态成熟 |
| 桌面端 | **Tauri 2 + Vue3 + Pinia + TypeScript** | 轻量、跨 Windows / macOS / Linux |
| 移动端 / 平板 | **Tauri 2（与桌面端同一代码库）** | 复用全部 Vue3 前端与 Rust core；Android 先行，iOS / iPad / 平板由同一构建 + 响应式布局覆盖，无需重写；Flutter 仅远期备选 |
| NAS / 服务器 | **Docker**（ARM64 / x86_64） | 部署简单 |
| Web 管理端 | Vue3（可选） | 与桌面端共享技术栈 |

## 九、核心模块

| 模块 | 职责 | 技术 |
|------|------|------|
| **Device Discovery** | 设备自动发现、自动上线 / 离线 | mDNS / Zeroconf / UDP 广播 |
| **Transport Engine** | 文件传输 | 局域网：TCP / QUIC；广域网：P2P、NAT 穿透、Relay |
| **Sync Engine** | 块同步、哈希校验、冲突解决、版本管理 | 参考 Syncthing |
| **Download Engine** | 多协议下载 | 第一阶段：Aria2 RPC；第二阶段：自研 |
| **BT Engine** | BT / Magnet 下载 | 第一阶段：Transmission RPC（V0.4 D2 设计修订自 qBittorrent）；第二阶段：libtorrent |
| **AI Engine** | 分类、标签、摘要、搜索 | 本地模型：llama.cpp + GGUF（Qwen / DeepSeek） |

## 十、安全架构

### 设备身份

- 首次配对时生成设备证书
- 每台设备拥有：设备指纹、公钥、私钥

### 加密

- 密钥协商：ECDH
- 数据加密：AES-256-GCM
- 通道：TLS 1.3

### 权限

设备信任 → 目录授权 → 分享授权，三级权限模型。

**设备信任分级（V0.2）**——第一级"设备信任"细化为四个层级，决定能否参与跨设备文件索引与同步：

| 层级 | 含义 | 入库 | 跨设备索引/同步 | 收文件 |
|------|------|------|------------------|--------|
| 完全信任 | 自己的多台设备 | ✅ | ✅ 双向 | 可设为免确认 |
| 朋友 | 朋友 / 家庭 / 团队 | ✅ | ❌（仅手动分享，见 AA Share） | 需确认 |
| 临时 | 单次收发对象 | ❌ | ❌ | 当次需确认 |
| 陌生 | 已发现未配对 | ❌（仅内存） | ❌ | 拒收 |

配对默认为"朋友"，用户在设置中把自己的设备升级为"完全信任"后才进入索引/同步。完整设计见 [SYNC_DESIGN.md](SYNC_DESIGN.md)。

## 十一、数据库

- **第一阶段**：SQLite（详见 [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)）
- **后续可选**：RocksDB（大规模文件索引场景）

保存内容：设备、任务、同步状态、标签、规则。

## 十二、插件架构

### Plugin API

所有高级能力通过统一的插件接口接入：

- Download / Sync / Share / AI / Storage / Notification

### 第三方扩展

第三方可开发：NAS 插件、云盘插件、模型插件、知识库插件。

## 十三、开源协议

**Apache License 2.0**

理由：允许商业使用、允许企业集成、提供专利保护、生态兼容性广。

> ⚠️ 注意：GPL 组件（如 Aria2、Transmission）仅通过 API / RPC 调用，不直接嵌入源码，避免许可证传染。

## 十四、开发路线

| 版本 | 目标 | 核心功能 |
|------|------|----------|
| **V0.1** | 完成 AA 核心闭环 | 设备发现、设备配对、文件发送、局域网传输（桌面三平台 + Android 实验版） |
| **V0.2** | 持续同步 | 文件夹同步、同步策略、历史记录、Android 体验完善 |
| **V0.3** | 突破局域网 | 广域网同步、NAT 穿透、Relay、分享链接 |
| **V0.4** | 统一文件入口 | 下载中心、BT 支持、统一任务中心 |
| **V0.5** | 数字资产管理 | AI 分类、自动归档、知识库 |
| **V1.0** | 完整连接平台 | 桌面端 + 移动端（含 iOS/iPad）+ NAS + Docker + 插件系统 |

详细排期见 [ROADMAP.md](ROADMAP.md)。

## 十五、最终目标

AA连接（AA4C）**不是**文件传输软件、同步软件、社区平台，而是：

> 一个**开源的跨平台设备连接平台**——通过连接设备，让文件传输、同步、分享、下载、归档等能力自然发生。

**一句话介绍**：AA连接（AA4C）——连接你的所有设备。

**一句话传播**：把文件 AA 给我。
